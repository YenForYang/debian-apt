<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE refentry PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
  "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
<!ENTITY % aptent SYSTEM "apt.ent"> %aptent;
<!ENTITY % aptverbatiment SYSTEM "apt-verbatim.ent"> %aptverbatiment;
<!ENTITY % aptvendor SYSTEM "apt-vendor.ent"> %aptvendor;
]>

<refentry>

 <refentryinfo>
   &apt-author.jgunthorpe;
   &apt-author.team;
   <author>
     &apt-name.dburrows;
     <contrib>Debug::* に関する最初のドキュメント</contrib>
     <email>dburrows@debian.org</email>
   </author>
   <!-- The last update date -->
&apt-email;
   &apt-product;
   
   <date>2018-12-10T00:00:00Z</date>
 </refentryinfo>
 
 <refmeta>
   <refentrytitle>apt.conf</refentrytitle>
   <manvolnum>5</manvolnum>
   <refmiscinfo class="manual">APT</refmiscinfo>
 </refmeta>
 
 
 <!-- Man page title -->
<refnamediv>
    <refname>apt.conf</refname>
    <refpurpose>APT の設定ファイル</refpurpose>
 </refnamediv>

 <refsect1><title>説明</title>
   <para><filename>/etc/apt/apt.conf</filename> は、APT
スイートの全ツールで使用するメイン設定ファイルです。しかし、間違いなくオプションを設定しておくだけの場所などではありません。そのため、APT
スイートは統一環境を提供するため、共通のコマンドラインパーサを共有しています。</para>

   <orderedlist>
      <para>APT ツールの起動時に、設定ファイルを以下の順番で読み込みます。</para>
      <listitem><para><envar>APT_CONFIG</envar> 環境変数で指定したファイル (存在する場合)</para></listitem>
      <listitem><para><literal>Dir::Etc::Parts</literal>
にあるすべてのファイルを英数字の昇順になります。ファイル名には拡張子がないか、"<literal>conf</literal>"
となっており、英数字、ハイフン (-)、アンダースコア (_)、ピリオド (.)
で構成されています。そうでなければ、<literal>Dir::Ignore-Files-Silently</literal>
設定リストのパターンに一致するもの以外に対して、ファイルを無視するという注意を APT が出力します。一致する場合は黙って無視します。</para></listitem>
      <listitem><para><literal>Dir::Etc::Main</literal> で指定される、メイン設定ファイル</para></listitem>
      <listitem><para>バイナリ固有の設定サブツリーに設定されたすべてのオプションは、ツリーのルートに移動されます。</para></listitem>
      <listitem><para>コマンドラインオプションにより、設定ディレクティブの上書きや設定ファイルの追加読み込みができます。</para></listitem>
   </orderedlist>
   </refsect1>
   <refsect1><title>構文</title>
   <para>設定ファイルは、機能グループごとに系統立てられたオプションを木構造で表します。オプションの指定は、2 つのコロンで区切ります。例えば
<literal>APT::Get::Assume-Yes</literal> は、APT ツールグループの Get
ツール用オプションです。オプションは、親グループからは継承しません。</para> 

   <para>設定言語の構文は、bind や dhcp のような ISC ツールをモデルにしています。<literal>//</literal>
で始まる行はコメントとして扱われます (無視)。同様に C/C++ のコメントのような <literal>/*</literal> と
<literal>*/</literal> の間もコメントとして扱います。いずれの行も、<literal>APT::Get::Assume-Yes
"true";</literal>
のような形式です。引用符と行末のセミコロンは必須です。値は1行でなければならず、文字列結合はありません。値の中にバックスラッシュや余計な引用符があってはいけません。オプション名は、英数字と
"/-:._+" が使用できます。以下のように波カッコを使うと、新しいスコープを開くことができます。</para>

<informalexample><programlisting>   
APT {
  Get {
    Assume-Yes "true";
    Fix-Broken "true";
  };
};
</programlisting></informalexample>

   <para>また適宜改行することで、より読みやすくなります。リストは、開いたスコープ、クォートで囲まれた 1
単語、そしてセミコロンと続けることで作成できます。セミコロンで区切って、複数のエントリを表せます。</para>

<informalexample><programlisting>   
DPkg::Pre-Install-Pkgs {"/usr/sbin/dpkg-preconfigure --apt";};
</programlisting></informalexample>

   <para>&configureindex; は一般的な設定ファイルのサンプルです。どのように設定するか参考になるでしょう。</para>

   <para>設定項目の名前にある文字の大小は、重要ではありません。そのため、前述の例を
<literal>dpkg::pre-install-pkgs</literal> とすることもできます。</para>

   <para>前述した <literal>DPkg::Pre-Install-Pkgs</literal>
の例で見られるように、リストを定義した場合、設定項目名はオプションになります。名前を指定しない場合、新しいエントリは、単純に新しいオプションをリストに追加します。名前を指定して同様にすると、あらゆる他の選択肢をオプションに再割り当てし、オプションを上書きできます。</para>

   <para><literal>#include</literal> (これは廃止予定でサポートしない他の実装もあります) と
<literal>#clear</literal> の 2 つの特別なコマンドがあります。<literal>#include</literal>
は指定したファイルを取り込みます。ファイル名がスラッシュで終わった場合には、そのディレクトリをすべて取り込みます。<literal>#clear</literal>
は、設定ツリーの一部を削除するのに使用します。指定した要素と、それ以下の要素を削除します
(これらの行も、セミコロンで終わる必要があることに注意してください)。</para>

   <para>
   <literal>#clear</literal> コマンドは、リストや完全なスコープを削除する唯一の方法です。スコープの再オープン (や後述する
<literal>::</literal>
スタイル)は、それまで書いたエントリを<emphasis>上書きしません</emphasis>。オプションでは新しい値を与えて上書きするしかありませんが、リストやスコープは上書きできません。クリアされるだけです。
   </para>

   <para>すべての APT ツールでは、コマンドラインで任意の設定ディレクティブを指定できる -o オプションが使用できます。構文は、完全なオプション名 (例:
<literal>APT::Get::Assume-Yes</literal>) に続き等号、その後オプションの新しい値となります。リスト名に続き
<literal>::</literal> を加えることで、リストを追加できます
(疑問に思われたように、スコープ構文はコマンドラインで使用できません)。</para>

   <para>
   <literal>::</literal> を用いたリストへの項目追加は、1 行で 1
項目の場合にのみ使用でき、スコープ構文と組み合わせるべきではないことに注意してください (スコープ構文には暗黙で
<literal>::</literal>
が追加されます)。両方の構文を同時に使用すると、「他のオプションと同様に名前と共に動作する、通常ではありえない名前
"<literal>::</literal>" のオプションである」といった、(残念なことに)
複数のユーザに渡るバグの引き金になります。たくさんの問題の中には、リストに追加しようと<emphasis>誤った</emphasis>構文で複数行書いたユーザが、このオプション
"<literal>::</literal>" を最後に割り当てようとして、正反対の結果を得るということも含まれます。APT
の次期バージョンでは、この誤用を検出すると、動作を停止しエラーを上げるようになります。そのため、APT
がこの件で明白にエラーを吐かない限りは、自分でそのような構文を修正してください。
   </para>
 </refsect1>

 <refsect1><title>APT グループ</title>
   <para>このオプショングループは、ツール全体に影響のある、一般的な APT の振る舞いを制御します。</para>

   <variablelist>
     <varlistentry><term><option>Architecture</option></term>
     <listitem><para>システムアーキテクチャ - ファイルを取得したり、パッケージリストを解析するときに使用するアーキテクチャをセットします。内部でのデフォルトは、apt
をコンパイルしたアーキテクチャです。</para></listitem>
     </varlistentry>

     <varlistentry><term><option>Architectures</option></term>
     <listitem><para>
     システムがサポートする、すべてのアーキテクチャです。例えば、<literal>amd64</literal>
(<literal>x86-64</literal> とも呼ばれます) 命令セットを実装した CPU は、<literal>i386</literal>
(<literal>x86</literal>)
命令セット用にコンパイルされたバイナリも実行できます。このリストは、ファイルを取得しパッケージリストを解析する際に利用されます。初期のデフォルト値は、常にシステムのネイティブアーキテクチャ
(<literal>APT::Architecture</literal>) です。<command>dpkg
--add-architecture</command> を用いて、外部アーキテクチャをデフォルトリストに追加します。
     </para></listitem>
     </varlistentry>

     <varlistentry><term><option>Compressor</option></term>
     <listitem><para>
     この設定項目ではどの圧縮形式をサポートするのか、その形式のサポートが apt
に直接組み込まれていない場合に圧縮と展開をどのように行えばいいのか、それとこの形式で何か圧縮するのにどれくらいコストがかかるのか、という値を定義します。1つの例として、以下の内容では拡張子が
<literal>.reversed</literal> のコストの低いファイルについて、ファイルの作成と保存に加えてダウンロードと展開を apt
に許可し、<command>rev</command> による圧縮と展開の際に渡すコマンドラインパラメータを追加しない設定となります:</para>
<informalexample><programlisting>
APT::Compressor::rev {
	Name "rev";
	Extension ".reversed";
	Binary "rev";
	CompressArg {};
	UncompressArg {};
	Cost "10";
};
</programlisting></informalexample>
     </listitem>
     </varlistentry>

     <varlistentry><term><option>Build-Profiles</option></term>
     <listitem><para>
     構築依存の解決にあたって有効になっているビルドプロファイルを名前空間の「<literal>profile.</literal>」接頭辞を取り除いたもの全ての一覧です。デフォルトではこの一覧は空です。&dpkg-buildpackage;
で利用される <envar>DEB_BUILD_PROFILES</envar> はこの一覧よりも優先されます。
     </para></listitem>
     </varlistentry>

     <varlistentry><term><option>Default-Release</option></term>
     <listitem><para>ひとつ以上のバージョンがある場合に、パッケージをインストールを行うデフォルトリリースです。リリース名や、コード名、リリースバージョンがあります。例えば
'stable', 'testing', 'unstable', '&debian-stable-codename;',
'&debian-testing-codename;', '4.0', '5.0*'  となります。&apt-preferences;
も参照してください。</para></listitem>
     </varlistentry>

     <varlistentry><term><option>Ignore-Hold</option></term>
     <listitem><para>保留パッケージの無視 - このグローバルオプションは、問題解決器に保留と指定したパッケージを無視します。</para></listitem>
     </varlistentry>

     <varlistentry><term><option>Clean-Installed</option></term>
     <listitem><para>デフォルトで有効です。on の時、autoclean 機能は、ダウンロードできなくなったパッケージを、すべてキャッシュから削除します。off
の場合、ローカルにインストールされているパッケージは、削除対象から外します。しかし、 APT
はキャッシュから削除したパッケージの再インストール方法を、直接提供するわけではないことに注意してください。</para></listitem>
     </varlistentry>

     <varlistentry><term><option>Immediate-Configure</option></term>
     <listitem><para>
     デフォルトは on です。&dpkg; の呼び出し失敗の影響を最小限にするため、インストール・更新操作の中で、APT
は不可欠パッケージや重要パッケージを、可能な限り早くインストールするようになります。このオプションを無効にすると、以下のように、重要パッケージを特別パッケージと同様に扱うようになります。パッケージ
A の展開とその設定の間に、パッケージ B や C などの関係ない、たくさんの他のパッケージの展開と設定が行えます。&dpkg;
の呼び出しに失敗した場合 (例: パッケージ B のメンテナスクリプトでエラー発生)、システムの状態は、パッケージ A
は展開したが未設定になります。そのため、A に依存したパッケージの動作が保証されなくなり、A への依存関係は、もう満たせません。
     </para><para>
     即時 (immediate) フラグは事前依存関係と等価なため、即時設定マーカも潜在的に、循環依存関係の問題に対して適用されます。理論上 APT
は、即時設定不能と認識し、中断し、操作を継続するためにこのオプションを一時的に無効にするよう提案できます。ここで言う「理論上」という言葉に注意してください。現実世界では、安定版ではないバージョンでも、この問題にはほとんど遭遇していませんし、問題のパッケージの依存関係が誤っているか、システムの状態がすでに破損している場合に発生していました。そのため、盲目的にこのオプションを無効にするべきではありません。前述のシナリオを解決する方法の、1つにしかすぎないのです。
     </para><para>
     このオプションを無効にして <literal>dist-upgrade</literal> のような大きな操作を実行する前に、パッケージに
<literal>install</literal>
を明示して、即時設定が行われないようにするべきです。ですが、動作の改善とアップグレードプロセスの修正のため、以下のバグリンクから、問題をディストリビューションと
APT チームにも報告していただきたいです。
     </para></listitem>
     </varlistentry>

     <varlistentry><term><option>Force-LoopBreak</option></term>
     <listitem><para>
     何をしようとしているのか<emphasis>本当に</emphasis>判っているのでなければ、絶対にこのオプションを有効にしないでください。不可欠
(essential) パッケージ同士で、競合 (Conflicts) /競合や競合/事前依存 (Pre-Depend)
のループに落ち込んだときに、不可欠パッケージを一時的に削除してループを抜けられるようにします。<emphasis>そんなループはあり得ないはずで、あるとすれば重大なバグです。</emphasis>このオプションは、<command>tar</command>,
<command>gzip</command>, <command>libc</command>, <command>dpkg</command>,
<command>dash</command> とそれらが依存しているパッケージ以外の不可欠パッケージで動作します。
     </para></listitem>
     </varlistentry>

     <varlistentry><term><option>Cache-Start</option></term><term><option>Cache-Grow</option></term><term><option>Cache-Limit</option></term>
     <listitem><para>APT は、バージョン 0.7.26
から、利用できる情報を格納するため、サイズ可変なメモリマップキャッシュファイルを使用します。<literal>Cache-Start</literal>
は、キャッシュサイズが増大していくヒントとして動作し、従って APT が起動時に要求するメモリ量です。デフォルト値は、20971520 バイト (~20
MB) です。この領域の量が、APT
に利用可能になっている必要があることに注意してください。そうでなければ、無様に失敗することになります。そのため、メモリに制限のあるデバイスで、たくさんのソースが設定され、それが増加していくシステムでは、この値をより低くしておくべきです。<literal>Cache-Grow</literal>
は、<literal>Cache-Start</literal>
が足りなくなったという領域定義イベントにより、どの程度キャッシュサイズを増加させるかを、バイト数で定義します。デフォルトは 1048576 (~1
MB) です。この値は全情報を格納できる量になるか、<literal>Cache-Limit</literal>
に達するまで、繰り返し適用されます。<literal>Cache-Limit</literal> のデフォルトは 0
で、無制限です。<literal>Cache-Grow</literal> に 0 をセットすると、キャッシュの自動増加を無効にします。
     </para></listitem>
     </varlistentry>

     <varlistentry><term><option>Build-Essential</option></term>
     <listitem><para>構築依存関係で不可欠なパッケージを定義します。</para></listitem>
     </varlistentry>

     <varlistentry><term><option>Get</option></term>
     <listitem><para>Get サブセクションは &apt-get; ツールを制御します。このオプションの詳細は &apt-get; の文書を参照してください。</para></listitem>
     </varlistentry>

     <varlistentry><term><option>Cache</option></term>
     <listitem><para>Cache サブセクションは &apt-cache; ツールを制御します。このオプションの詳細は &apt-cache; の文書を参照してください。</para></listitem>
     </varlistentry>

     <varlistentry><term><option>CDROM</option></term>
     <listitem><para>CDROM サブセクションは &apt-cdrom; ツールを制御します。このオプションの詳細は &apt-cdrom; の文書を参照してください。</para></listitem>
     </varlistentry>
   </variablelist>
 </refsect1>

 <refsect1><title>Acquire グループ</title>
   <para>オプションの <literal>Acquire</literal> グループは、パッケージのダウンロードやダウンロードに関して責任を持つ、様々な「取得
(acquire) 方法」 を制御します (&sources-list; も参照)。</para>

   <variablelist>
     <varlistentry><term><option>Check-Date</option></term>
	 <listitem><para>
	 Security related option defaulting to true, enabling time-related
checks. Disabling it means that the machine's time cannot be trusted, and
APT will hence disable all time-related checks, such as
<option>Check-Valid-Until</option> and verifying that the Date field of a
release file is not in the future.
	 </para></listitem>
     </varlistentry>

     <varlistentry><term><option>Max-FutureTime</option></term>
	 <listitem><para>Maximum time (in seconds) before its creation (as indicated by the
<literal>Date</literal> header) that the <filename>Release</filename> file
should be considered valid.  The default value is <literal>10</literal>.
Archive specific settings can be made by appending the label of the archive
to the option name. Preferably, the same can be achieved for specific
&sources-list; entries by using the <option>Date-Max-Future</option> option
there.
	 </para></listitem>
     </varlistentry>
     <varlistentry><term><option>Check-Valid-Until</option></term>
     <listitem><para>
     セキュリティ関連のオプションで、デフォルトは true です。Release
ファイルの有効期限検証により、長期間のリプレイ攻撃を防ぎます。さらに、例えばユーザが、もう更新されないミラーサイトを識別するのを支援します。しかしこの機能は、ユーザのシステム時計が正確であることに依存しています。アーカイブメンテナは、<literal>Valid-Until</literal>
ヘッダがある Release ファイルを作成するよう推奨されていますが、このヘッダを付与しない、またはより厳密な値が必要な場合、以下の
<literal>Max-ValidTime</literal> オプションを使用できます。&sources-list; エントリの
<option>Check-Valid-Until</option>
オプションは、グローバルオーバーライドを使う代わりに選択的にチェックを無効にすることが好ましいです。
     </para></listitem>
     </varlistentry>

     <varlistentry><term><option>Max-ValidTime</option></term>
	 <listitem><para><filename>Release</filename> ファイルを作成してから (<literal>Date</literal>
ヘッダ)、有効であるとみなせる最大時間 (秒数) です。Release ファイル自体に <literal>Valid-Until</literal>
ヘッダがある場合、その 2 つのうち、より早い日付が有効期間として使用されます。デフォルト値は <literal>0</literal>
で「無期限」を表します。オプション名にアーカイブのラベルを付与することで、アーカイブ固有の設定を作成できます。または、<option>Valid-Until-Max</option>
オプションを使用して特定の &sources-list; エントリで同じことを達成できます。
	 </para></listitem>
     </varlistentry>

     <varlistentry><term><option>Min-ValidTime</option></term>
	 <listitem><para><filename>Release</filename> ファイルを作成してから (<literal>Date</literal>
ヘッダ)、有効であるとみなせる最小時間 (秒数) です。<literal>Valid-Until</literal>
がある頻繁に更新されるアーカイブの、ほとんど更新されない (ローカル)
ミラーを使用する場合に、完全に有効期限チェックを無効にする代わりに使用してください。オプション名にアーカイブのラベルを付与することで、アーカイブ固有の設定を作成できます。または、<option>Valid-Until-Min</option>
オプションを使用して特定の &sources-list; エントリで同じことを達成できます。
	 </para></listitem>
     </varlistentry>

     <varlistentry><term><option>AllowTLS</option></term>
     <listitem><para>
     Allow use of the internal TLS support in the http method. If set to false,
this completely disables support for TLS in apt's own methods (excluding the
curl-based https method). No TLS-related functions will be called anymore.
     </para></listitem>
     </varlistentry>

     <varlistentry><term><option>PDiffs</option></term>
	 <listitem><para>(<filename>Packages</filename> のような)
インデックス全体をダウンロードするのではなく、<literal>PDiffs</literal>
と呼ばれる差分をダウンロードしようとします。デフォルトでは True です。または、<option>PDiffs</option>
オプションを使用して特定の &sources-list; エントリまたはインデックスファイルでこれを設定できます。</para>
	 <para>PDiffs の使用を制限するふたつのサブオプションがあります。<literal>FileLimit</literal>
では、ファイルを更新するのに、PDiff ファイルをダウンロードできる最大数を指定します。一方、<literal>SizeLimit</literal>
は、対象ファイルのサイズに対して、全パッチサイズの最大パーセンテージを指定します。どちらの制限を超えても、パッチをダウンロードする代わりに、完全なファイルをダウンロードします。
	 </para></listitem>
     </varlistentry>

     <varlistentry><term><option>By-Hash</option></term>
	 <listitem><para>(<filename>Packages</filename> のような)
インデックス全体をダウンロードするのではなく、<literal>PDiffs</literal>
と呼ばれる差分をダウンロードしようとします。デフォルトでは True です。または、<option>By-Hash</option>
オプションを使用して特定の &sources-list; エントリまたはインデックスファイルでこれを設定できます。
	 </para></listitem>
     </varlistentry>

     <varlistentry><term><option>Queue-Mode</option></term>
     <listitem><para>キューモード - <literal>Queue-Mode</literal> は、APT
がどのように並列接続を行うか、<literal>host</literal> か <literal>access</literal>
で指定できます。<literal>host</literal> は、ターゲットホストごとに 1
接続を開きます。<literal>access</literal> は、URI タイプごとに 1 接続を開きます。</para></listitem>
     </varlistentry>

     <varlistentry><term><option>Retries</option></term>
     <listitem><para>リトライの回数を設定します。0 でない場合、APT は失敗したファイルに対して、与えられた回数だけリトライを行います。</para></listitem>
     </varlistentry>

     <varlistentry><term><option>Source-Symlinks</option></term>
     <listitem><para>ソースアーカイブのシンボリックリンクを使用します。true がセットされているとき、可能ならコピーの代わりにシンボリックリンクを張ります。true
がデフォルトです。</para></listitem>
     </varlistentry>

     <varlistentry><term><option>http</option> <option>https</option></term>
     <listitem><para>The options in these scopes configure APTs acquire transports for the
protocols HTTP and HTTPS and are documented in the &apt-transport-http; and
&apt-transport-https; manpages respectively.</para></listitem>
     </varlistentry>

     <varlistentry><term><option>ftp</option></term>
     <listitem><para>
     <literal>ftp::Proxy</literal> は、FTP URI
を使用する際のデフォルトプロキシを設定します。<literal>ftp://[[user][:pass]@]host[:port]/</literal>
という標準形で表します。ホストごとのプロキシを、<literal>ftp::Proxy::&lt;host&gt;</literal>
という形で指定できます。この時、プロキシを使用しないという意味の特殊キーワード <literal>DIRECT</literal>
も使用できます。上記の設定をなにも指定しないと、環境変数 <envar>ftp_proxy</envar> を使用します。FTP
プロキシを使用するには、設定ファイルに <literal>ftp::ProxyLogin</literal>
スクリプトを設定する必要があります。このエントリには、接続する際にプロキシサーバに送信するコマンドを設定します。どのようにするのかは
&configureindex; の例を参照してください。URI
を構成するコンポーネントに対応する置換変数は、<literal>$(PROXY_USER)</literal>,
<literal>$(PROXY_PASS)</literal>, <literal>$(SITE_USER)</literal>,
<literal>$(SITE_PASS)</literal>, <literal>$(SITE)</literal>,
<literal>$(SITE_PORT)</literal> です。</para>

     <para><literal>timeout</literal>
オプションは、この方法でのタイムアウトまでの時間を設定します。これには、接続のタイムアウトとデータのタイムアウトが含まれています。</para>

     <para>設定のいくつかは、パッシブモードを制御するものです。一般的に、パッシブモードのままにしておく方が安全で、ほぼどんな環境でも動作します。しかしある状況下では、パッシブモードが無効のため、代わりにポートモード
FTP を使用する必要があります。この設定は、プロキシを通る接続や特定のホストへの接続全般に有効です
(設定例はサンプル設定ファイルを参照してください)。</para>

     <para>環境変数 <envar>ftp_proxy</envar> に HTTP URL を指定すると FTP over HTTP
のプロキシが利用可能になります。構文は前述の http
についての説明を参照してください。設定ファイルの中でこれをセットすることはできません。また、効率が悪いため FTP over HTTP
を使用するのは推奨しません。</para>

     <para><literal>ForceExtended</literal> の設定は RFC2428 の <literal>EPSV</literal>
コマンドと <literal>EPRT</literal> コマンドの使用を制御します。デフォルトでは false で、コントロールコネクションが
IPv6 の時にのみ、このコマンドを使用するということです。これを true にセットすると、IPv4
コネクションでも、強制的にこのコマンドを使用します。ほとんどの FTP サーバは RFC2428 をサポートしていないことに注意してください。</para></listitem>
     </varlistentry>

     <varlistentry><term><option>cdrom</option></term>
     <listitem><para>
     <literal>cdrom</literal> メソッドを利用する URI
では、設定できるオプションはマウントポイントだけです。<filename>/etc/fstab</filename>
で設定されているように、CD-ROM (または DVD など) ドライブのマウントポイントを
<literal>cdrom::Mount</literal> に設定しなければなりません。マウントポイントが fstab
に記述できない場合、かわりにマウント・アンマウントコマンドも使用できます。構文は、<literal>cdrom</literal> ブロックを
<literallayout>/cdrom/::Mount "foo";</literallayout>
の形で記述します。スラッシュを後につけるのが重要です。アンマウントコマンドは UMount で指定することができます。
     </para></listitem>
     </varlistentry>

     <varlistentry><term><option>gpgv</option></term>
     <listitem><para>
     GPGV URI 用の唯一のオプションは、gpgv
に渡す追加パラメータのオプションである、<literal>gpgv::Options</literal> です。
     </para></listitem>
     </varlistentry>

     <varlistentry><term><option>CompressionTypes</option></term>
     <listitem><para>acquire メソッドが理解できる、圧縮法のリストです。<filename>Packages</filename>
のようなファイルにはさまざまな圧縮形式が利用できます。デフォルトで acquire メソッドは、<command>xz</command> や
<command>gzip</command>
等の一般的な多くの圧縮形式で展開、再圧縮できます。この設定項目ではサポートする圧縮形式を参照、変更でき、サポートする圧縮形式をさらに追加することもできます
(関連項目 <option>APT::Compressor</option>)。<synopsis>Acquire::CompressionTypes::<replaceable>FileExtension</replaceable> "<replaceable>Methodname</replaceable>";</synopsis>
     </para><para>また、<literal>Order</literal>
サブグループを使用して、取得システムが、圧縮ファイルのダウンロードを試みる順番を定義できます。取得システムは先頭のものを試行し、エラーが発生すると次の圧縮タイプのダウンロードを開始します。そのため、どれかを他のものより優先したい場合、単にそのタイプを先頭に追加してください。まだ追加していないタイプは、リストの最後に追加されます。つまり、<synopsis>Acquire::CompressionTypes::Order:: "gz";</synopsis> とすると、<command>gzip</command>
で圧縮されたファイルを他のどれよりも優先的に使用します。もし <command>xz</command> を
<command>gzip</command> や <command>bzip2</command>
よりも優先するべきなら、設定を以下のようにしてください。<synopsis>Acquire::CompressionTypes::Order { "xz"; "gz"; };</synopsis>
<literal>bz2</literal> は自動的に追加されるため、明示する必要はありません。</para>
     <para>実行時に <literal>Dir::Bin::<replaceable>Methodname</replaceable></literal>
をチェックすることに注意してください。このオプションが設定されていてその形式のサポートが apt
に直接組み込まれていない場合、ファイルがある場合に、この方法しか使われなくなります。例えば、bzip2 メソッド (内蔵)
の設定は以下になります。<literallayout>Dir::Bin::bzip2 "/bin/bzip2";</literallayout>
また、コマンドラインに指定した一覧のエントリは設定ファイルに指定したものの後、デフォルトエントリの前に追加されることに注意してください。この場合、設定ファイルに指定した内容よりも前に指定するには、オプションを直接
(一覧スタイルでなく) 指定してください。これは定義されている一覧を上書きするのではなく、このタイプを先頭に付加するだけです。</para>
     <para>特殊なタイプ <literal>uncompressed</literal>
は、非圧縮ファイルを優先するために使用できますが、ほとんどのアーカイブは非圧縮ファイルを提供しないため、ほとんどローカルミラーでのみ有効になることに注意してください。</para></listitem>
     </varlistentry>

     <varlistentry><term><option>GzipIndexes</option></term>
	 <listitem><para>
	 <literal>gzip</literal> 圧縮インデックス (Packages, Sources, Translations)
をダウンロードする際、ローカルで展開せずに、gzip
で圧縮したままにします。これにより、かなりディスク領域を節約できますが、ローカルパッケージキャッシュを構築する際に、CPU
の能力を余計に消費します。デフォルトでは false です。
	 </para></listitem>
     </varlistentry>

     <varlistentry><term><option>Languages</option></term>
     <listitem><para>Languages サブセクションは、<filename>Translation</filename> ファイルをダウンロードし、APT
が説明の翻訳を表示しようとする場合の挙動を制御します。APT はリストの先頭にある言語で、まず有効な説明を表示しようとします。Languages は
短い形式や長い形式の言語コードで、定義できます。すべてのアーカイブが、全言語の <filename>Translation</filename>
ファイルを、提供しているわけではないことに注意してください。長い言語コードは特に見かけません。</para>
     <para>デフォルトのリストには "environment" と "en" があります。"<literal>environment</literal>"
はここでは特殊な意味があります。これは実行時に、<literal>LC_MESSAGES</literal>
環境変数から取得した言語コードに置換されます。また、このリストには、同じコードが2度現れないように確認してください。<literal>LC_MESSAGES</literal>
が "C" に設定されているだけの場合、<filename>Translation-en</filename> ファイルを (利用可能であれば)
使用します。強制的に APT が Translation
ファイルを使用しないようにするには、<literal>Acquire::Languages=none</literal>
と設定してください。"<literal>none</literal>" はもうひとつの特殊な意味を持つコードで、適切な
<filename>Translation</filename> ファイルの検索を中止します。環境から実際の言語を指定されなかった場合、この値を用いて
APT に翻訳をダウンロードさせます。そのため、以下の設定例では、英語ロケールの場合 "en, de" の順になり、ドイツ語ロケールの場合 "de,
en" の順になります。"fr" はダウンロードされますが、フランス語ロケール ("fr, de, en" の順になる) でないと、APT
が使用しないことに注意してください。<programlisting>Acquire::Languages { "environment"; "de"; "en"; "none"; "fr"; };</programlisting></para>
     <para>注意: 異なる環境 (例: 異なるユーザや他のプログラム) で APT
を実行したことによる問題を防ぐため、<filename>/var/lib/apt/lists/</filename> にあるすべての
Translation ファイルを、リストの最後 (暗黙の "<literal>none</literal>" の後) に追加します。</para>
     </listitem>
     </varlistentry>

     <varlistentry><term><option>ForceIPv4</option></term>
	 <listitem><para>
         ダウンロード時に IPv4 プロトコルだけを使うように強制します。
	 </para></listitem>
     </varlistentry>

     <varlistentry><term><option>ForceIPv6</option></term>
	 <listitem><para>
         ダウンロード時に IPv6 プロトコルだけを使うように強制します。
	 </para></listitem>
     </varlistentry>

     <varlistentry><term><option>MaxReleaseFileSize</option></term>
	 <listitem><para>
           Release/Release.gpg/InRelease ファイルの最大ファイルサイズです。デフォルトは 10MB です。
	 </para></listitem>
     </varlistentry>

     <varlistentry><term><option>EnableSrvRecords</option></term>
	 <listitem><para>
           apt が 接続するための代替サーバを選択するために RFC 2782 で指定された DRS SRV
サーバレコードを使う場合は、このオプションを制御します。デフォルトは "true" です。
	 </para></listitem>
     </varlistentry>

     <varlistentry><term><option>AllowInsecureRepositories</option></term>
	 <listitem><para>
	   十分なセキュリティ情報を提供していないリポジトリからのデータファイル読み込みを行う更新操作を許可します。デフォルトは「<literal>false</literal>」です。概念や影響、代替手段については
&apt-secure; で説明しています。
	 </para></listitem>
     </varlistentry>

     <varlistentry><term><option>AllowWeakRepositories</option></term>
	 <listitem><para>
	   セキュリティ情報を提供してはいるものの暗号的に十分に強いものとは見なされなくなっている場合に、そういったリポジトリからのデータファイル読み込みを行う更新操作を許可します。デフォルトは「<literal>false</literal>」です。概念や影響、代替手段については
&apt-secure; で説明しています。
	 </para></listitem>
     </varlistentry>

     <varlistentry><term><option>AllowDowngradeToInsecureRepositories</option></term>
	 <listitem><para>
	   以前に GPG
署名されたリポジトリが、更新操作中に署名なしにすることを許可します。以前に信頼されたリポジトリの有効な署名が存在しない場合、更新を拒否します。このオプションは、この保護を無効にするために使用することができます。ほぼ確実に、これを有効にすることはありません。デフォルトは
<literal>false</literal> です。概念や影響、代替手段については &apt-secure; で説明しています。
	 </para></listitem>
     </varlistentry>

     <varlistentry><term><option>Changelogs::URI</option> スコープ</term>
	<listitem><para>
	  URI
は、それらを取得する場所から知られている場合は、変更履歴の取得のみ行うことができます。好ましいリリースファイルは、「変更履歴」フィールドでこのことを示します。これが利用できない場合、<literal>Acquire::Changelogs::URI::Label::<replaceable>LABEL</replaceable></literal>
または
<literal>Acquire::Changelogs::URI::Origin::<replaceable>ORIGIN</replaceable></literal>
オプションが存在し、そしてこの値が取られてる場合は、リリースファイルの Label/Origin
フィールドが確認するために使用されます。リリースファイル内の値は、<literal>Acquire::Changelogs::URI::Override::Label::<replaceable>LABEL</replaceable></literal>
または
<literal>Acquire::Changelogs::URI::Override::Origin::<replaceable>ORIGIN</replaceable></literal>
で上書きできます。そのパッケージ固有のデータが <literal>@CHANGEPATH@</literal>
プレースホルダに置き換えられている場合以外は、値はテキストファイルへの正常な URI にする必要があります。値は次の通りです:
1. パッケージがコンポーネント (例えば、<literal>main</literal>)
からのものである場合、それ以外の場合は省略されている最初の部分です、2. ソースパッケージ名の最初の文字。ただし、ソースパッケージ名が
'<literal>lib</literal>' で始まる場合を除きます (その場合は 最初の 4
文字になります)。3. 完全なソースパッケージ名。4. 再び完全な名前および 5. ソースのバージョン。(存在する場合は)
最初、第二、第三、第四の部分はスラッシュ ('<literal>/</literal>') で区切られ、第四と第五の部分の間はアンダースコア
('<literal>_</literal>') です。このソースから変更履歴を取得することができないことを示す特別な値
'<literal>no</literal>' がこのオプションで利用可能です。この場合、可能であれば別のソースが試行されます。
	</para></listitem>
    </varlistentry>

   </variablelist>
 </refsect1>

 <refsect1><title>バイナリユーザ設定</title>
    <para>特に、<command>apt</command>
のバイナリの導入では、<option>APT::Get::Show-Versions</option> が <command>apt</command>
と同様に <command>apt-get</command>
に効果を与えるように、特定のバイナリのみに影響を与えるように見えるオプションであっても、特定のバイナリのために特定のオプションを設定することは有用であり得ます。
    </para>
    <para>特定のバイナリ用のオプション設定は、<option>Binary::<replaceable>specific-binary</replaceable></option>
スコープ内でオプションを設定して達成することができます。 <command>apt</command>
用の<option>APT::Get::Show-Versions</option>
オプションの設定は、例えば、<option>Binary::apt::APT::Get::Show-Versions</option>
設定の代わりに実行されることができます。</para>
    <para>さらに上記の説明の節に見られるように、コマンドライン上のオプション自体またはコマンドラインからロードされた設定ファイル中でバイナリ固有のオプションを設定することはできません。</para>
 </refsect1>

 <refsect1><title>ディレクトリ</title>

   <para><literal>Dir::State</literal>
セクションは、ローカル状態情報に関するディレクトリを保持します。<literal>lists</literal>
は、ダウンロードしたパッケージ一覧を格納するディレクトリで、<literal>status</literal> は &dpkg;
の状態ファイルの名前を表します。<literal>preferences</literal> は APT の
<filename>preferences</filename> ファイルの名前です。<literal>Dir::State</literal>
には、<filename>/</filename> や <filename>./</filename>
で始まらないサブアイテムすべてに、前に付加するデフォルトディレクトリを含んでいます。</para>

   <para><literal>Dir::Cache</literal>
は、ローカルキャッシュ情報に関する場所を格納しています。これは、ダウンロード済アーカイブの場所を示す
<literal>Dir::Cache::archives</literal> と同様に、<literal>srcpkgcache</literal>
と <literal>pkgcache</literal>
のパッケージキャッシュの場所になります。<literal>pkgcache</literal> や
<literal>srcpkgcache</literal> に <literal>""</literal>
をセットすることで、キャッシュの生成を無効にできます。これにより起動時に遅くなりますが、ディスク容量を節約できます。おそらく、srcpkgcache
よりも pkgcache を無効にすることが多いと思います。<literal>Dir::State</literal>
と同様、<literal>Dir::Cache</literal> はデフォルトディレクトリを含んでいます。</para>

   <para><literal>Dir::Etc</literal> は設定ファイルの場所を格納しています。<literal>sourcelist</literal>
はソースリストの場所を示し、<literal>main</literal>
はデフォルトの設定ファイルです。(<envar>APT_CONFIG</envar> で設定ファイルを指定された場合のみ、この設定の効果があります)</para>

   <para><literal>Dir::Parts</literal>
設定は、指定されたディレクトリから、字句単位の全ての設定断片を読みこみます。これを設定した後に、メイン設定ファイルをロードします。</para>

   <para>バイナリプログラムは <literal>Dir::Bin</literal>
で指定します。<literal>Dir::Bin::Methods</literal>
はメソッドハンドラの場所を指定し、<literal>gzip</literal>, <literal>bzip2</literal>,
<literal>lzma</literal>, <literal>dpkg</literal>, <literal>apt-get</literal>
<literal>dpkg-source</literal> <literal>dpkg-buildpackage</literal>,
<literal>apt-cache</literal> はそれぞれプログラムの場所を指定します。</para>

   <para>
     設定項目 <literal>RootDir</literal> は特別な意味を持ちます。設定されていると、すべてのパスは
<emphasis>絶対パスとして指定しても</emphasis><literal>RootDir</literal>
からの相対パスとなります。そのため例えば、<literal>RootDir</literal> が
<filename>/tmp/staging</filename>
にセットされており、<literal>Dir::State::status</literal> が
<filename>/var/lib/dpkg/status</filename> にセットされている場合、status ファイルを
<filename>/tmp/staging/var/lib/dpkg/status</filename>
から探します。相対バスのみをプレフィックスにしたい場合は、代わりに <literal>Dir</literal> を設定します。
   </para>

   <para>
      <literal>Ignore-Files-Silently</literal> リストは、断片ディレクトリの解析中に、APT
が黙って無視をするファイルを指定します。デフォルトではそれぞれ、<literal>.disabled</literal>,
<literal>~</literal>, <literal>.bak</literal>,
<literal>.dpkg-[a-z]+</literal>
で終わるものが、黙って無視されます。最後のデフォルト値を見ればわかる通り、このパターンには正規表現を使用できます。
   </para>
 </refsect1>
 
 <refsect1><title>DSelect での APT</title>
   <para>   
   &dselect; メソッドで APT を使用する際、いくつかの設定ディレクティブでデフォルトの動作を制御します。これは
<literal>DSelect</literal> セクション以下にあります。</para>
   
   <variablelist>
     <varlistentry><term><option>Clean</option></term>
     <listitem><para>キャッシュクリーンモード - この値は <literal>always</literal>, <literal>prompt</literal>,
<literal>auto</literal>, <literal>pre-auto</literal>,
<literal>never</literal> のうちひとつを取ります。<literal>always</literal> と
<literal>prompt</literal> は更新後、全パッケージをキャッシュから削除します。<literal>prompt</literal>
(デフォルト) では条件付きで削除します。<literal>auto</literal> はダウンロード不能パッケージ
(例えば新バージョンで置き換えられたもの) を削除します。<literal>pre-auto</literal>
はこの動作を、新パッケージをダウンロードする直前に行います。</para></listitem>
     </varlistentry>

     <varlistentry><term><option>options</option></term>
     <listitem><para>この変数の内容は、install 時のコマンドラインオプションと同様に &apt-get; に渡されます。</para></listitem>
     </varlistentry>

     <varlistentry><term><option>Updateoptions</option></term>
     <listitem><para>この変数の内容は、update 時のコマンドラインオプションと同様に &apt-get; に渡されます。</para></listitem>
     </varlistentry>

     <varlistentry><term><option>PromptAfterUpdate</option></term>
     <listitem><para>true の場合、&dselect; の [U]pdate 実行時に、続行のためのプロンプトを毎回表示します。デフォルトはエラーが発生した場合のみです。</para></listitem>
     </varlistentry>
   </variablelist>
 </refsect1>
 
 <refsect1><title>APT が &dpkg; を呼ぶ方法</title>
   <para>いくつかの設定項目で APT がどのように &dpkg; を呼び出すかを制御できます。<literal>DPkg</literal>
セクションにあります。</para>

   <variablelist>
     <varlistentry><term><option>options</option></term>
     <listitem><para>&dpkg; に渡すオプションのリストです。オプションは、リスト記法を使用して指定しなければなりません。また、各リストは単一の引数として &dpkg;
に渡されます。</para></listitem>
     </varlistentry>

     <varlistentry><term><option>Path</option></term>
     <listitem><para>This is a string that defines the <envar>PATH</envar> environment variable
used when running dpkg. It may be set to any valid value of that environment
variable; or the empty string, in which case the variable is not changed.</para></listitem>
     </varlistentry>
     
     <varlistentry><term><option>Pre-Invoke</option></term><term><option>Post-Invoke</option></term>
     <listitem><para>&dpkg; を呼び出す前後で実行するシェルコマンドのリストです。<literal>options</literal>
のようにリスト記法で指定しなければなりません。コマンドは <filename>/bin/sh</filename> を通して呼び出され、何か問題があれば
APT が異常終了します。</para></listitem>
     </varlistentry>

     <varlistentry><term><option>Pre-Install-Pkgs</option></term>
     <listitem><para>&dpkg; を呼び出す前に実行するシェルコマンドのリストです。<literal>options</literal>
のようにリスト記法で指定しなければなりません。コマンドは <filename>/bin/sh</filename>
を通して呼び出され、何か問題があれば、APT は異常終了します。APT はインストールしようとする全 .deb
ファイルのファイル名を、要求したファイルディスクリプタについて1 行ずつ、デフォルトではコマンドの標準入力に送ります。</para>

     <para>このプロトコルのバージョン 2 は、要求されたファイルディスクリプタを介してより多くの情報を送信します: テキスト <literal>VERSION
2</literal> の行、APT 設定空間、ファイル名とバージョン情報を持つパッケージ処理のリスト。</para>

     <para>各設定ディレクティブ行は、<literal>key=value</literal> 形式です。特殊文字 (<literal>key</literal>
中のイコール記号、改行、非印字可能文字、引用符、およびパーセント記号、および <literal>value</literal>
中の改行、非印字可能文字、およびパーセント記号) は、%
エンコードされます。リストは、同じキーを持つ複数の<literal>key::=value</literal>
行で表されます。設定セクションは、空白行で終わります。</para>

     <para>バージョン 2 ではパッケージアクション行は 5 つのフィールドで構成されています: パッケージ名
(例え外国だとしてもアーキテクチャ資格なしで)、旧バージョン、バージョン変更の方向 (&lt; アップグレード、&gt; ダウングレード、=
変化なし)、新バージョン、処理。バージョンなしの場合は、バージョンフィールドはすべて "-" です
(例えば、初めてパッケージをインストール時、バージョンなしは、任意の実際のバージョンよりも前として扱われます。そのため、<literal>- &lt;
1.23.4</literal>はアップグレードとなります)。パッケージが設定されている場合のアクションフィールドは "**CONFIGURE**"
で、削除されている場合は "**REMOVE**" 、解凍されている場合は .deb ファイルのファイル名です。</para>

     <para>バージョン 3 では、各バージョンフィールドの後に、このバージョンのアーキテクチャが続きます。バージョンなしの場合は "-" 、そして
MultiArch 型 "same"、"foreign"、"allowed" または "none" を示すフィールド。"none"
は単に互換性を維持するために残された、誤った型名であることに注意してください。それは"no"
と読まれるべきで、ユーザは両方をサポートすることをお勧めします。</para>

     <para>コマンド <literal><replaceable>cmd</replaceable></literal> で利用するプロトコルのバージョンは
<literal>DPkg::Tools::options::<replaceable>cmd</replaceable>::Version</literal>
の設定により選択でき、デフォルトはバージョン 1 となっています。リクエストしたバージョンを APT
がサポートしていない場合はサポートしている最大のバージョンの情報を代わりに送ります。
     </para>

     <para>情報の送信に利用するファイルディスクリプタは
<literal>DPkg::Tools::options::<replaceable>cmd</replaceable>::InfoFD</literal>
によりリクエストでき、デフォルトは <literal>0</literal> で標準入力を指定します。バージョン 0.9.11
から利用可能となりました。環境変数 <envar>APT_HOOK_INFO_FD</envar>
を見ることでこのオプションに対応していることを検出でき、それには確認として利用しているファイルディスクリプタの番号が収録されています。</para>
     </listitem>
     </varlistentry>

     <varlistentry><term><option>Run-Directory</option></term>
     <listitem><para>APT は &dpkg; を呼び出す前にこのディレクトリに移動します。デフォルトは <filename>/</filename> です。</para></listitem>
     </varlistentry>

     <varlistentry><term><option>Build-options</option></term>
     <listitem><para>これらのオプションは、パッケージのコンパイル時に &dpkg-buildpackage;
に渡されます。デフォルトでは署名を無効にし、全バイナリを生成します。</para></listitem>
     </varlistentry>

     <varlistentry><term><option>DPkg::ConfigurePending</option></term>
     <listitem><para>このオプションがセットされると、&dpkg; にすべての必要な設定とトリガを扱わせるように、APT は <command>dpkg
--configure --pending</command> を呼び出します。このオプションは、デフォルトで有効となっていますが、APT
を複数回連続して実行する場合 (インストーラなど)
には、無効にしたほうが便利かもしれません。このシナリオでは、最後以外のすべての実行で、無効にできます。</para></listitem>
     </varlistentry>
   </variablelist>
 </refsect1>

 <refsect1>
   <title>Periodic オプションと Archives オプション</title>
   <para>オプションの <literal>APT::Periodic</literal> グループと
<literal>APT::Archives</literal>
グループは、<literal>/usr/lib/apt/apt.systemd.daily</literal> スクリプトを使用して、apt
の定期更新の挙動を設定します。このオプションのドキュメントは、このスクリプトの先頭を参照してください。
   </para>
 </refsect1>

 <refsect1>
   <title>デバッグオプション</title>
   <para>
     <literal>Debug::</literal> セクションのオプションを有効にすると、<literal>apt</literal>
ライブラリを利用するプログラムの標準エラー出力に、デバッグ情報を送るようになったり、主に <literal>apt</literal>
の挙動をデバッグするのに便利な、特殊モードを有効にしたりできます。普通のユーザには、ほとんどのオプションは興味がないでしょうが、以下のものは興味を引くかもしれません。

     <itemizedlist>
       <listitem>
	 <para>
	   <literal>Debug::pkgProblemResolver</literal> は、<literal>dist-upgrade,
upgrade, install, remove, purge</literal> で行われた判断についての出力を有効にします。
	 </para>
       </listitem>

       <listitem>
	 <para>
	   <literal>Debug::NoLocking</literal> はファイルロックをすべて無効にします。これは、非 root ユーザとして操作
(例えば <literal>apt-get -s install</literal>) を行う場合に使用します。
	 </para>
       </listitem>

       <listitem>
	 <para>
	   <literal>Debug::pkgDPkgPM</literal> は <literal>apt</literal> が実際に &dpkg;
を起動する際の各コマンドラインを表示します。
	 </para>
       </listitem>

       <listitem>
	 <para>
	   <!-- TODO: provide a
	   motivating example, except I haven't a clue why you'd want
	   to do this. -->
<literal>Debug::IdentCdrom</literal> は CD-ROM ID にある statfs データを含めないようにします。  
	 </para>
       </listitem>
     </itemizedlist>
   </para>

   <para>
     以下は apt に対するデバッグオプションのすべてです。
   </para>

   <variablelist>
     <varlistentry>
       <term><option>Debug::Acquire::cdrom</option></term>

       <listitem>
	 <para>
	   <literal>cdrom://</literal> ソースへのアクセスに関する情報を出力します。
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::Acquire::ftp</option></term>

       <listitem>
	 <para>
	   FTP を用いたパッケージのダウンロードに関する情報を出力します。
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::Acquire::http</option></term>

       <listitem>
	 <para>
	   HTTP を用いたパッケージのダウンロードに関する情報を出力します。
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::Acquire::https</option></term>

       <listitem>
	 <para>
	   HTTPS を用いたパッケージのダウンロードに関する情報を出力します。
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::Acquire::gpgv</option></term>

       <listitem>
	 <para>
	   <literal>gpg</literal> を用いた暗号署名の検証に関する情報を出力します。
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::aptcdrom</option></term>

       <listitem>
	 <para>
	   CD-ROM にあるパッケージ集合に対して、アクセスするプロセスについての情報を出力します。
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::BuildDeps</option></term>
       <listitem>
	 <para>
	   &apt-get; での構築依存関係解決のプロセスを説明します。
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::Hashes</option></term>
       <listitem>
	 <para>
	   <literal>apt</literal> ライブラリが生成した、暗号化ハッシュを出力します。
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::IdentCDROM</option></term>
       <listitem>
	 <para>
	   CD-ROM の ID を生成する際に <literal>statfs</literal> という、CD-ROM
ファイルシステムにある使用済・未使用ブロックの数からの情報を含めないようにします。
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::NoLocking</option></term>
       <listitem>
	 <para>
	   ファイルのロックをすべて無効にします。例えば、同時にふたつの <quote><literal>apt-get
update</literal></quote> を実行できるようになります。
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::pkgAcquire</option></term>

       <listitem>
	 <para>
	   グローバルダウンロードキューに対する項目の追加・削除の際にログを出力します。
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::pkgAcquire::Auth</option></term>
       <listitem>
	 <para>
	   ダウンロードしたファイルのチェックサムや暗号署名の確認に関する、状態メッセージやエラーを出力します。
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::pkgAcquire::Diffs</option></term>
       <listitem>
	 <para>
	   パッケージインデックスリスト差分のダウンロード・適用時の、情報やエラーを出力します。
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::pkgAcquire::RRed</option></term>

       <listitem>
	 <para>
	   インデックス全体ではなくインデックス差分のダウンロードの際に、apt パッケージリストへのパッチ適用に関する情報を出力します。
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::pkgAcquire::Worker</option></term>

       <listitem>
	 <para>
	   実際のダウンロードを行う際の、サブプロセスとのやりとりをログに出力します。
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::pkgAutoRemove</option></term>

       <listitem>
	 <para>
	   パッケージの自動インストールや、不要パッケージの削除に関するイベントを、ログに出力します。
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::pkgDepCache::AutoInstall</option></term>
       <listitem>
	 <para>
	   依存関係解決のために、どのパッケージが自動的にインストールされたかのデバッグメッセージを生成します。これは完全な
<literal>apt</literal> 依存関係解決プログラム
(<literal>Debug::pkgProblemResolver</literal> を参照) ではなく、例えば <literal>apt-get
install</literal> で実行された、初期の自動インストール経路に対応しています。
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::pkgDepCache::Marker</option></term>
       <listitem>
        <para>
           ProblemResolver
が動作する際に、一時保留・インストール・削除としてマークされたパッケージに関する、デバッグメッセージを生成します。各追加・削除は追加アクションのトリガとなり、これをオリジナルのエントリの下に、空白
2 個でインデントして表示します。各行は、<literal>MarkKeep</literal>,
<literal>MarkDelete</literal>, <literal>MarkInstall</literal>
となり、<literal>package-name &lt;a.b.c -&gt; d.e.f | x.y.z&gt;
(section)</literal> が続きます。ここで、<literal>a.b.c</literal>
は、そのパッケージの現在のバージョン、<literal>d.e.f</literal>は、インストール予定のバージョン、<literal>x.y.z</literal>
はより新しいけれどインストール対象外のバージョン (pin のスコアが低いため)
です。後のふたつは、存在しない、ないしインストール中のバージョンと同じ場合、省略されます。<literal>section</literal>
はパッケージが現れるセクション名です。
        </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::pkgDPkgPM</option></term>
       <listitem>
	 <para>
	   &dpkg; 起動時に、起動した際の正確なコマンドラインを出力します。引数は空白で区切られます。
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::pkgDPkgProgressReporting</option></term>
       <listitem>
	 <para>
	   状態ファイルディスクリプタに、&dpkg; から受信したすべてのデータと、そのデータを解析中に発生したエラーを出力します。
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::pkgOrderList</option></term>

       <listitem>
	 <para>
	   <literal>apt</literal> が &dpkg; にパッケージを渡す順番を決める、アルゴリズムのトレースを生成します。
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::pkgPackageManager</option></term>

       <listitem>
	 <para>
	   &dpkg; を呼び出す際に、実行手順を追跡した状態メッセージを出力します。
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::pkgPolicy</option></term>

       <listitem>
	 <para>
	   起動時の各パッケージの優先度を表示します。
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::pkgProblemResolver</option></term>

       <listitem>
	 <para>
	   依存関係解決システムの実行内容を追跡します (これは複雑な依存関係の問題に遭遇した場合にのみ、適用されます)。
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::pkgProblemResolver::ShowScores</option></term>
       <listitem>
        <para>
          全インストール済みパッケージの、pkgProblemResolver
が計算したスコアを一覧表示します。パッケージの説明は、<literal>Debug::pkgDepCache::Marker</literal>
で説明したものと同じです。
        </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::sourceList</option></term>

       <listitem>
	 <para>
	   <filename>/etc/apt/vendors.list</filename> から読み込んだ、ベンダの情報を出力します。
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::RunScripts</option></term>
       <listitem>
        <para>
          aptのフックにより呼び出される外部コマンドを表示します。これには例えば、設定オプション
<literal>DPkg::{Pre,Post}-Invoke</literal> や
<literal>APT::Update::{Pre,Post}-Invoke</literal> があります。
        </para>
       </listitem>
     </varlistentry>



   <!-- 2009/07/11 Currently used nowhere. The corresponding code
is commented.
     <varlistentry>

       <term><literal>Debug::Vendor</literal></term>

       <listitem>
	 <para>
	   Print information about each vendor.
	 </para>
       </listitem>
     </varlistentry>
-->
</variablelist>
 </refsect1>
 
 <refsect1><title>サンプル</title>
   <para>&configureindex; は、全利用可能オプション値のサンプルを参照できる、設定ファイルのサンプルです。</para>
 </refsect1>
 
 <refsect1><title>ファイル</title>
   <variablelist>
      &file-aptconf;
   </variablelist>
 </refsect1>
 
 <refsect1><title>関連項目</title>
   <para><!-- ? reading apt.conf -->
&apt-cache;, &apt-config;, &apt-preferences;.</para>
 </refsect1>

 &manbugs;
 
 <refsect1><title>&translation-title;</title>
   <para>&translation-holder;</para>
   <para>&translation-english;</para>
 </refsect1>
</refentry>


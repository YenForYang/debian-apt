<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE refentry PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
  "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
<!ENTITY % aptent SYSTEM "apt.ent"> %aptent;
<!ENTITY % aptverbatiment SYSTEM "apt-verbatim.ent"> %aptverbatiment;
<!ENTITY % aptvendor SYSTEM "apt-vendor.ent"> %aptvendor;
]>

<refentry>

 <refentryinfo>
   &apt-author.jgunthorpe;
   &apt-author.team;
   <author>
     &apt-name.dburrows;
     <contrib>ursprüngliche Dokumentation von Debug::*.</contrib>
     <email>dburrows@debian.org</email>
   </author>
   <!-- The last update date -->
&apt-email;
   &apt-product;
   
   <date>2018-12-10T00:00:00Z</date>
 </refentryinfo>
 
 <refmeta>
   <refentrytitle>apt.conf</refentrytitle>
   <manvolnum>5</manvolnum>
   <refmiscinfo class="manual">APT</refmiscinfo>
 </refmeta>
 
 
 <!-- Man page title -->
<refnamediv>
    <refname>apt.conf</refname>
    <refpurpose>Konfigurationsdatei für APT</refpurpose>
 </refnamediv>

 <refsect1><title>Beschreibung</title>
   <para><filename>/etc/apt/apt.conf</filename> ist die Hauptkonfigurationsdatei, die
alle Werkzeuge der APT-Programmsammlung gemeinsam nutzen, es ist jedoch
beileibe nicht der einzige Ort, an dem Optionen gesetzt werden können. Die
Programmsammlung benutzt außerdem einen gemeinsamen Befehlszeilenauswerter,
um eine einheitliche Umgebung bereitzustellen.</para>

   <orderedlist>
      <para>Wenn ein APT-Werkzeug startet, wird es die Konfigurationsdateien in der
folgenden Reihenfolge lesen:</para>
      <listitem><para>die Datei, die durch die Umgebungsvariable <envar>APT_CONFIG</envar>
angegeben wird (falls gesetzt)</para></listitem>
      <listitem><para>alle Dateien in <literal>Dir::Etc::Parts</literal> in aufsteigender
alphanumerischer Reihenfolge, die entweder keine oder
»<literal>conf</literal>« als Dateinamenserweiterung haben und die nur
alphanumerische Zeichen, Bindestriche (-), Unterstriche (_) und Punkte (.)
enthalten. Andernfalls wird APT einen Hinweis ausgeben, dass es eine Datei
ignoriert hat, falls die Datei nicht auf ein Muster in der
Konfigurationsliste <literal>Dir::Ignore-Files-Silently</literal> passt – in
diesem Fall wird sie stillschweigend ignoriert.</para></listitem>
      <listitem><para>die Hauptkonfigurationsdatei, die durch <literal>Dir::Etc::main</literal>
angegeben wird</para></listitem>
      <listitem><para>Alle in der programmspezifischen Konfigurationsunterbaumstruktur gesetzten
Optionen werden in die Wurzel der Baumstruktur verschoben.</para></listitem>
      <listitem><para>Die Befehlszeilenoptionen werden angewendet, um die Konfigurationsdirektiven
außer Kraft zu setzen oder um sogar weitere Konfigurationsdateien zu laden.</para></listitem>
   </orderedlist>
   </refsect1>
   <refsect1><title>Syntax</title>
   <para>Die Konfigurationsdatei ist in einem Baum mit Optionen organisiert, die in
funktionellen Gruppen organisiert sind. Optionsspezifikation wird mit einer
doppelten Doppelpunktschreibweise angegeben, zum Beispiel ist
<literal>APT::Get::Assume-Yes</literal> eine Option innerhalb der
APT-Werkzeuggruppe für das Werkzeug Get. Optionen werden nicht von ihren
Elterngruppe geerbt.</para> 

   <para>Syntaktisch ist die Konfigurationssprache dem nachempfunden, was die
ISC-Werkzeuge, wie bind und dhcp, benutzen. Zeilen, die mit
<literal>//</literal> beginnen, werden als Kommentar betrachtet (und
ignoriert), ebenso wie jeglicher Text zwischen <literal>/*</literal> und
<literal>*/</literal>, wie bei C/C++-Kommentaren. Jede Zeile hat die Form
<literal>APT::Get::Assume-Yes "true";</literal>. Die Anführungszeichen und
abschließenden Strichpunkte werden benötigt. Der Wert muss in einer Zeile
stehen und es gibt keine Möglichkeit, Zeichenketten aneinander zu
hängen. Werte dürfen keine Rückwärtsschrägstriche oder zusätzliche
Anführungszeichen enthalten. Optionsnamen werden aus alphanumerischen
Zeichen und den Zeichen »/-:._+« gebildet. Ein neuer Geltungsbereich kann
mit geschweiften Klammern geöffnet werden, wie:</para>

<informalexample><programlisting>   
APT {
  Get {
    Assume-Yes "true";
    Fix-Broken "true";
  };
};
</programlisting></informalexample>

   <para>mit eingefügten Zeilenumbrüchen, um es leserlicher zu gestalten. Listen
können erstellt werden, indem ein Geltungsbereich geöffnet wird und eine
einzelne, von Anführungszeichen, denen ein Semikolon folgt, eingeschlossene
Zeichenkette eingefügt wird. Es können mehrere Einträge eingefügt werden,
jeweils getrennt durch ein Semikolon.</para>

<informalexample><programlisting>   
DPkg::Pre-Install-Pkgs {"/usr/sbin/dpkg-preconfigure --apt";};
</programlisting></informalexample>

   <para>Im Allgemeinen bietet die Beispielkonfigurationsdatei &configureindex; eine
gute Anleitung, wie dies aussehen könnte.</para>

   <para>Die Groß- und Kleinschreibung ist in Namen der Konfigurationselemente nicht
von Bedeutung, deshalb könnten Sie im vorherigen Beispiel auch
<literal>dpkg::pre-install-pkgs</literal> benutzen.</para>

   <para>Namen für die Konfigurationselemente sind optional, wenn eine Liste, wie sie
im Beispiel <literal>DPkg::Pre-Install-Pkgs</literal> oberhalb gesehen
werden kann, definiert ist. Wenn Sie keinen neuen Namen angeben, wird ein
neuer Eintrag der Liste lediglich eine neue Option hinzufügen. Wenn Sie
einen Namen eingeben, können Sie die Option, wie jede andere Option, außer
Kraft setzen, indem Sie der Option erneut einen neuen Wert zuweisen.</para>

   <para>Es sind zwei Spezialbefehle definiert: <literal>#include</literal> (was
missbilligt ist und von alternativen Implementierungen nicht unterstützt
wird) und <literal>#clear</literal>: <literal>#include</literal> wird die
angegebene Datei einfügen, außer, wenn der Dateiname mit einem Schrägstrich
endet, in diesem Fall wird das ganze Verzeichnis
eingefügt. <literal>#clear</literal> wird benutzt, um einen Teil des
Konfigurationsbaums zu löschen. Das angegebene Element und alle davon
darunterliegenden Elemente werden gelöscht. (Beachten Sie, dass diese Zeilen
auch mit einem Semikolon enden müssen.)</para>

   <para>
   Der <literal>#clear</literal>-Befehl ist die einzig Art, eine Liste oder
einen kompletten Geltungsbereich zu löschen. Erneutes Öffnen eines
Geltungsbereichs (oder die unten beschriebene Syntax mit angehängten
<literal>::</literal>) wird vorherige geschriebene Einträge
<emphasis>nicht</emphasis> außer Kraft setzen. Optionen können nur außer
Kraft gesetzt werden, indem ein neuer Wert an sie adressiert wird – Listen
und Geltungsbereiche können nicht außer Kraft gesetzt, sondern nur bereinigt
werden.
   </para>

   <para>Alle APT-Werkzeuge akzeptieren eine Option -o, die es erlaubt, eine
beliebige Konfigurationsdirektive auf der Befehlszeile anzugeben. Die Syntax
ist ein vollständiger Optionsname (<literal>APT::Get::Assume-Yes</literal>
zum Beispiel), gefolgt von einem Gleichheitszeichen und dann dem neuen Wert
der Option. Um ein neues Element an eine Liste anzuhängen,  hängen Sie ein
<literal>::</literal> an den Namen der Liste an. (Wie Sie vielleicht erraten
haben: Die Geltungsbereichssyntax kann nicht auf der Befehlszeile benutzt
werden.)</para>

   <para>
   Beachten Sie, dass das Anhängen von Elementen an eine Liste mittels
<literal>::</literal> nur für ein Element pro Zeile funktioniert, Sie
sollten es nicht nicht in Verbindung mit einer Geltungsbereichssyntax
benutzen (die »<literal>::</literal>« implizit hinzufügt). Die Benutzung der
Syntax von beiden zusammen wird einen Fehler auslösen, auf den sich einige
Benutzer ungünstigerweise verlassen: eine Option mit dem unüblichen Namen
»<literal>::</literal>«, die sich wie jede andere Option mit einem Namen
verhält. Dies leitet viele Probleme ein; zum einen werden Benutzer, die
mehrere Zeilen in dieser <emphasis>falschen</emphasis> Syntax in der
Hoffnung, etwas an die Liste anzuhängen, schreiben, das Gegenteil erreichen,
da nur die letzte Zuweisung zu dieser Option »<literal>::</literal>« benutzt
wird. Zukünftige APT-Versionen werden Fehler ausgeben und die Arbeit
stoppen, wenn sie auf diese falsche Verwendung stoßen. Korrigieren Sie
deshalb nun solche Anweisungen, solange sich APT nicht explizit darüber
beklagt.
   </para>
 </refsect1>

 <refsect1><title>Die APT-Gruppe</title>
   <para>Diese Gruppe von Optionen steuert das allgemeine Verhalten von APT, ebenso
wie es die Optionen für alle Werkzeuge enthält.</para>

   <variablelist>
     <varlistentry><term><option>Architecture</option></term>
     <listitem><para>Systemarchitektur; setzt die Architektur, die benutzt wird, wenn Dateien
heruntergeladen und Paketlisten ausgewertet werden. Die interne Vorgabe ist
die Architektur für die APT kompiliert wurde.</para></listitem>
     </varlistentry>

     <varlistentry><term><option>Architectures</option></term>
     <listitem><para>
     alle Architekturen, die das System unterstützt. Zum Beispiel sind CPUs, die
den <literal>amd64</literal>-Befehlssatz implementieren (auch
<literal>x86-64</literal> genannt), ebenso in der Lage, Programme
auszuführen, die für den <literal>i386</literal>-Befehlssatz
(<literal>x86</literal>) kompiliert wurden. Diese Liste wird benutzt, wenn
Dateien abgerufen und Paketlisten ausgewertet werden. Die anfängliche
Vorgabe ist immer die native Architektur des Systems
(<literal>APT::Architecture</literal>) und fremde Architekturen werden der
vorgegebenen Liste hinzugefügt, wenn sie per <command>dpkg
--print-architectures</command> registriert werden.
     </para></listitem>
     </varlistentry>

     <varlistentry><term><option>Compressor</option></term>
     <listitem><para>
     Dieser Gültigkeitsbereich definiert, welche Komprimierungsformate
unterstützt werden, wie Komprimierung und Dekomprimierung durchgeführt
werden können, falls die Unterstützung für dieses Format nicht direkt in APT
eingebaut wurde und einen Kostenwert, der anzeigt, wie aufwändig es ist,
etwas in dieses Format zu komprimieren. Als Beispiel würde es der folgende
Konfigurationsabschnitt APT erlauben, Dateien herunterzuladen und zu
dekomprimieren sowie mit der kostengünstigen <literal>.reversed</literal>
Dateiendung zu erzeugen und zu speichern. Dadurch werden sie ohne
zusätzliche Befehlszeilenparameter zum Komprimieren und Dekomprimieren an
den Befehl <command>rev</command> übergeben:</para>
<informalexample><programlisting>
APT::Compressor::rev {
	Name "rev";
	Extension ".reversed";
	Binary "rev";
	CompressArg {};
	UncompressArg {};
	Cost "10";
};
</programlisting></informalexample>
     </listitem>
     </varlistentry>

     <varlistentry><term><option>Build-Profiles</option></term>
     <listitem><para>
     Liste aller Bauprofile ohne das Namensraum-Präfix
»<literal>profile.</literal>«, die für die Auflösung der Bauabhängigkeiten
aktiviert sind. Standardmäßig ist diese Liste
leer. <envar>DEB_BUILD_PROFILES</envar> setzt so, wie es durch
&dpkg-buildpackage; benutzt wird, die Listenschreibweise außer Kraft.
     </para></listitem>
     </varlistentry>

     <varlistentry><term><option>Default-Release</option></term>
     <listitem><para>Standard-Release von dem Pakete installiert werden, wenn mehr als eine
Version verfügbar ist. Enthält Release-Name, Codename oder
Release-Version. Beispiele: »stable«, »testing, »unstable«,
»&debian-stable-codename;«, »&debian-testing-codename;«, »4.0«,
»5.0*«. Siehe auch &apt-preferences;.</para></listitem>
     </varlistentry>

     <varlistentry><term><option>Ignore-Hold</option></term>
     <listitem><para>Halten von Paketen ignorieren. Diese globale Option veranlasst den
Problemlöser, gehaltene Pakete beim Treffen von Entscheidungen zu
ignorieren.</para></listitem>
     </varlistentry>

     <varlistentry><term><option>Clean-Installed</option></term>
     <listitem><para>standardmäßig auf on (ein). Wenn es auf on gesetzt wird, wird die
automatische Bereinigungsfunktion alle Pakete entfernen, die nicht länger
aus dem Zwischenspeicher heruntergeladen werden können. Wenn es auf off
gesetzt wird, dann werden außerden die Pakete, die lokal installiert sind,
vom Bereinigen ausgeschlossen – beachten Sie jedoch, dass APT keine direkten
Möglichkeiten bereitstellt, um sie erneut zu installieren.</para></listitem>
     </varlistentry>

     <varlistentry><term><option>Immediate-Configure</option></term>
     <listitem><para>
     ist standardmäßig aktiviert, was APT veranlassen wird, essenzielle und
wichtige Pakete so bald wie möglich in einer
Installations-/Aktualisierungstransaktion zu installieren, um die
Auswirkungen eines scheiternden &dpkg;-Aufrufs zu begrenzen. Falls diese
Option deaktiviert ist, betrachtet APT ein wichtiges Paket auf die gleiche
Weise wie ein zusätzliches Paket: Zwischen dem Entpacken des Pakets A und
seiner Konfiguration können viele andere Entpack- oder Konfigurationsaufrufe
für andere, nicht zugehörige Pakete B, C usw. liegen. Falls dies zum
Scheitern des &dpkg;-Aufrufs führt (z.B. weil Betreuerskripte des Pakets B
einen Fehler erzeugen), führt dies zu einem Systemstatus, in dem Paket A
entpackt, aber nicht konfiguriert ist, daher ist nicht länger gewährleistet,
dass irgendein Paket, das von A abhängt, weiter funktioniert, da seine
Abhängigkeit von A nicht länger erfüllt wird.
     </para><para>
     Außerdem wird die unmittelbare Konfigurationsmarkierung im potenziell
problematischen Fall zirkulärer Abhängigkeiten angewendet, da eine
Abhängigkeit mit der Unmittelbarmarkierung einer Vorabhängigkeit
entspricht. Theoretisch ermöglicht dies APT, eine Situation zu erkennen, in
der es keine unmittelbare Konfiguration durchführen kann, abzubrechen und
dem Benutzer vorzuschlagen, dass er die Option zeitweise deaktivieren soll,
um ein Fortsetzen der Transaktion zu ermöglichen. Beachten Sie hier das Wort
»theoretisch«; im wahren Leben ist dieses Problem selten in instabilen
Distributionsversionen aufgetreten und es wurde durch falsche Abhängigkeiten
des fraglichen Pakets verursacht oder durch ein System in einem bereits
beschädigten Zustand; daher sollten Sie diese Option nicht blind
deaktivieren, da das zuvor erwähnte Szenario nicht das einzige Problem ist,
das es überhaupt erst verhindern kann.
     </para><para>
     Bevor eine umfangreiche Transaktion wie <literal>dist-upgrade</literal> mit
dieser ausgeschalteten Option ausgeführt wird, sollten Sie versuchen,
explizit <literal>install</literal> für das Paket auszuführen, das APT nicht
unmittelbar konfigurieren kann. Stellen Sie aber bitte sicher, dass Sie Ihr
Problem außerdem an Ihre Distribution und an das APT-Team mit dem Verweis
zur Fehlermeldung unten melden, so dass sie an der Verbesserung und
Korrektur des Upgrade-Prozesses arbeiten können.
     </para></listitem>
     </varlistentry>

     <varlistentry><term><option>Force-LoopBreak</option></term>
     <listitem><para>
     Schalten Sie diese Option niemals ein, außer wenn Sie
<emphasis>wirklich</emphasis> wissen, was Sie tun. Es erlaubt APT, temporär
ein essenzielles Paket zu entfernen, um eine Conflicts/Conflicts- oder
Conflicts/Pre-Depends-Schleife zwischen zwei essenziellen Paketen zu
durchbrechen. <emphasis>Solch eine Schleife sollte niemals existieren und
ist ein schwerwiegender Fehler</emphasis>. Diese Option wird funktionieren,
wenn die essenziellen Pakete nicht <command>tar</command>,
<command>gzip</command>, <command>libc</command>, <command>dpkg</command>,
<command>dash</command> oder etwas, was davon abhängt, sind.
     </para></listitem>
     </varlistentry>

     <varlistentry><term><option>Cache-Start</option></term><term><option>Cache-Grow</option></term><term><option>Cache-Limit</option></term>
     <listitem><para>APT benutzt seit Version 0.7.26 eine größenveränderbare, speicherbasierte
Zwischenspeicherdatei, um verfügbare Informationen zu
speichern. <literal>Cache-Start</literal> dient als Hinweis, auf welche
Größe der Zwischenspeicher wachsen wird und enthält daher den Betrag,
welchen Speicher APT beim Start abruft. Die Vorgabe ist 20971520 Byte (~20
MB). Beachten Sie, dass diese Speichermenge für APT verfügbar sein muss, da
es sonst unschön scheitert. Für Geräte mit eingeschränktem Speicher sollten
diese Werte vermindert werden, während sie für Systeme mit vielen
konfigurierten Quellen erhöht werden sollte. <literal>Cache-Grow</literal>
definiert in Byte mit einer Vorgabe von 1048576 (~1 MB) um wieviel die Größe
des Zwischenspeichers vergößert werden soll, falls der durch
<literal>Cache-Start</literal> vorreservierte nicht ausreicht. Dieser Wert
wird wieder und wieder verwandt bis entweder der Zwischenspeicher groß genug
ist, um alle Informationen zu speichern oder die Zwischenspeichergröße das
<literal>Cache-Limit</literal> erreicht. Vorgabe für
<literal>Cache-Limit</literal> ist 0, was bedeutet, dass es kein Limit
gibt. Falls <literal>Cache-Grow</literal> auf 0 gesetzt ist, kann der
Zwischenspeicher nicht automatisch wachsen.
     </para></listitem>
     </varlistentry>

     <varlistentry><term><option>Build-Essential</option></term>
     <listitem><para>definiert, welche Pakete als essenzielle Bauabhängigkeiten betrachtet
werden.</para></listitem>
     </varlistentry>

     <varlistentry><term><option>Get</option></term>
     <listitem><para>Der Get-Unterabschnitt steuert das &apt-get;-Werkzeug. Lesen Sie bitte
dessen Dokumentation, um weitere Informationen über die dortigen Optionen zu
erhalten.</para></listitem>
     </varlistentry>

     <varlistentry><term><option>Cache</option></term>
     <listitem><para>Der Cache-Unterabschnitt steuert das &apt-cache;-Werkzeug. Lesen Sie bitte
dessen Dokumentation, um weitere Informationen über die dortigen Optionen zu
erhalten.</para></listitem>
     </varlistentry>

     <varlistentry><term><option>CDROM</option></term>
     <listitem><para>Der CDROM-Unterabschnitt steuert das &apt-cdrom;-Werkzeug. Lesen Sie bitte
dessen Dokumentation, um weitere Informationen über die dortigen Optionen zu
erhalten.</para></listitem>
     </varlistentry>
   </variablelist>
 </refsect1>

 <refsect1><title>Die Beschaffungsgruppe</title>
   <para>Die <literal>Acquire</literal>-Gruppe der Optionen steuert das Herunterladen
von Paketen ebenso wie die verschiedenen »Beschaffungsmethoden«, die für das
Herunterladen selbst zuständig sind (siehe auch &sources-list;).</para>

   <variablelist>
     <varlistentry><term><option>Check-Date</option></term>
	 <listitem><para>
	 Sicherheitsbezogene Optionen sind auf »true« voreingestellt, wodurch
zeitbezogene Prüfungen aktiviert sind. Deaktivieren bedeutet, dass der Zeit
auf der Maschine nicht vertraut wird. Dann wird APT alle zeitbezogenen
Prüfungen wie <option>Check-Valid-Until</option> und die Prüfung, ob das
Datumsfeld einer Release-Datei in der Zukunft liegt, deaktivieren.
	 </para></listitem>
     </varlistentry>

     <varlistentry><term><option>Max-FutureTime</option></term>
	 <listitem><para>maximale Zeit (in Sekunden) vor ihrer Erzeugung (die in der Kopfzeile
<literal>Date</literal> angegeben ist), die die Datei
<filename>Release</filename> als gültig angesehen wird. Der voreingestellte
Wert ist <literal>10</literal>. Archivspezifische Einstellungen können durch
Anhängen der Archivbezeichnung an den Optionsnamen vorgenommen
werden. Vorzugsweise kann dasselbe für bestimmte &sources-list;-Einträge
erreicht werden, indem dort die Option <option>Date-Max-Future</option>
benutzt wird.
	 </para></listitem>
     </varlistentry>
     <varlistentry><term><option>Check-Valid-Until</option></term>
     <listitem><para>
     Die sicherheitsbezogene Option wird als »true« vorgegeben. Damit werden
Wiederholungsangriffe über einen längeren Zeitraum hinweg verhindert, indem
der Überprüfung der Datei Release ein Ablaufdatum gegeben
wird. Beispielsweise wird Benutzern damit auch geholfen, länger nicht
aktualisierte Spiegel zu erkennen – diese Funktion hängt jedoch von der
Richtigkeit der Zeiteinstellung auf dem Benutzersystem ab. Archivbetreuer
sind aufgefordert, Release-Dateien mit der Kopfzeile
<literal>Valid-Until</literal> zu erstellen. Falls sie das nicht tun oder
ein strengerer Wert gewünscht wird, kann die Option
<literal>Max-ValidTime</literal> benutzt werden (siehe unten). Der Option
<option>Check-Valid-Until</option> von &sources-list;-Einträgen zum
wahlweisen Deaktivieren der Prüfung sollte der Vorzug gegeben werden,
anstatt dies global mit dieser Einstellung außer Kraft zu setzen.
     </para></listitem>
     </varlistentry>

     <varlistentry><term><option>Max-ValidTime</option></term>
	 <listitem><para>maximale Zeit (in Sekunden) nach ihrer Erzeugung (die in der Kopfzeile
<literal>Date</literal> angegeben ist), die die Datei
<filename>Release</filename> als gültig angesehen wird. Falls die
Release-Datei selbst eine <literal>Valid-Until</literal>-Kopfzeile enthält,
wird das frühere der beiden Daten als Ablaufdatum verwandt. Vorgabe ist
<literal>0</literal>, was für »für immer gültig« steht. Archivspezifische
Einstellungen können durch Anhängen der Archivbezeichnung an den
Optionsnamen vorgenommen werden. Vorzugsweise kann dasselbe für bestimmte
&sources-list;-Einträge erreicht werden, indem dort die Option
<option>Valid-Until-Max</option> benutzt wird.
	 </para></listitem>
     </varlistentry>

     <varlistentry><term><option>Min-ValidTime</option></term>
	 <listitem><para>minimale Zeit (in Sekunden), nach der Erzeugung (die in der Kopfzeile
<literal>Date</literal> angegeben ist), die die Datei
<filename>Release</filename> als gültig angesehen wird. Benutzen Sie dies,
falls Sie einen selten aktualisierten (lokalen) Spiegel eines häufiger
aktualisierten Archivs mit einer <literal>Valid-Until</literal>-Kopfzeile
haben, anstatt die Überprüfung des Ablaufdatums komplett zu
deaktivieren. Archivspezifische Einstellungen können und sollten durch
Anhängen der Archivbezeichnung an den Optionsnamen vorgenommen
werden. Vorzugsweise kann dasselbe für bestimmte &sources-list;-Einträge
erreicht werden, indem dort die Option <option>Valid-Until-Min</option>
benutzt wird.
	 </para></listitem>
     </varlistentry>

     <varlistentry><term><option>AllowTLS</option></term>
     <listitem><para>
     ermöglicht die Verwendung interner TLS-Unterstützung in der
HTTP-Methode. Falls auf »false« gesetzt, deaktiviert dies vollständig die
Unterstützung für TLS in den eigenen Methoden von APT (außer bei den
cURL-basierten HTTPS-Methoden). Es werden keine TLS-bezogenen Funktionen
mehr aufgerufen.
     </para></listitem>
     </varlistentry>

     <varlistentry><term><option>PDiffs</option></term>
	 <listitem><para>versucht Unterschiede, die <literal>PDiffs</literal> genannt werden, für
Indexe (wie <filename>Packages</filename>-Dateien) statt der kompletten
Dateien herunterzuladen. Vorgabe ist »true«. Vorzugsweise kann dies für
bestimmte &sources-list;-Einträge oder Index-Dateien gesetzt werden, indem
dort die Option <option>PDiffs</option> benutzt wird.</para>
	 <para>Es sind außerdem zwei Unteroptionen verfügbar, um die Benutzung von PDiffs
zu begrenzen: <literal>FileLimit</literal> kann verwandt werden, um die
maximale Anzahl von PDiff-Dateien anzugeben, die zum Aktualisieren einer
Datei heruntergeladen werden sollen. Andererseits gibt
<literal>SizeLimit</literal> die maximale Prozentzahl der Größe aller
Patches im Vergleich zur Zieldatei an. Wenn eine dieser Begrenzungen
überschritten wird, wird die komplette Datei anstelle der Patche
heruntergeladen.
	 </para></listitem>
     </varlistentry>

     <varlistentry><term><option>By-Hash</option></term>
	 <listitem><para>versucht Indexe über einen URI der aus der Hash-Summe der erwarteten Datei
gebildet wurde, statt über einen gut bekannten stabilen Dateinamen
herunterzuladen. Standardmäßig »true«, wird aber automatisch deaktiviert,
falls die Quelle anzeigt, dass es nicht unterstützt wird. Der Gebrauch kann
mit dem speziellen Wert »force« erzwungen werden. Vorzugsweise kann dies für
bestimmte &sources-list;-Einträge oder Index-Dateien gesetzt werden, indem
dort die Option <option>By-Hash</option> benutzt wird.
	 </para></listitem>
     </varlistentry>

     <varlistentry><term><option>Queue-Mode</option></term>
     <listitem><para>Warteschlangenmodus; <literal>Queue-Mode</literal> kann entweder
<literal>host</literal> oder <literal>access</literal> sein, wodurch
festgelegt wird, wie APT ausgehende Verbindungen
parallelisiert. <literal>host</literal> bedeutet, dass eine Verbindung pro
Zielrechner geöffnet wird, <literal>access</literal> bedeutet, dass eine
Verbindung pro URI-Art geöffnet wird.</para></listitem>
     </varlistentry>

     <varlistentry><term><option>Retries</option></term>
     <listitem><para>Anzahl der auszuführenden erneuten Versuche. Wenn dies nicht Null ist, wird
APT fehlgeschlagene Dateien die angegebene Anzahl erneut versuchen.</para></listitem>
     </varlistentry>

     <varlistentry><term><option>Source-Symlinks</option></term>
     <listitem><para>benutzt symbolische Verweise für Quellarchive. Falls dies auf »true« gesetzt
ist, werden Quellarchive, wenn möglich, symbolisch verknüpft, anstatt
kopiert zu werden. Die Voreinstellung ist »true«.</para></listitem>
     </varlistentry>

     <varlistentry><term><option>http</option> <option>https</option></term>
     <listitem><para>Die Optionen in diesen Bereichen konfigurieren, wie APT Transporte über die
Protokolle HTTP und HTTPS abwickelt. Sie sind in den Handbuchseiten
&apt-transport-http; beziehungsweise &apt-transport-https; dokumentiert.</para></listitem>
     </varlistentry>

     <varlistentry><term><option>ftp</option></term>
     <listitem><para>
     <literal>ftp::Proxy</literal> setzt den Standard-Proxy, der für FTP-URIs
benutzt werden soll. Er wird in der Standardform
<literal>ftp://[[Benutzer][:Passwort]@]Rechner[:Port]/</literal>
angegeben. Proxys pro Rechner können außerdem in der Form
<literal>ftp::Proxy::&lt;Rechner&gt;</literal> angegeben werden. Hierbei
bedeutet das spezielle Schlüsselwort <literal>DIRECT</literal>, dass keine
Proxys benutzt werden. Falls keine der obigen Einstellungen angegeben wurde,
wird die Umgebungsvariable <envar>ftp_proxy</envar> benutzt. Um einen
FTP-Proxy zu benutzen, müssen Sie in der Konfigurationsdatei das Skript
<literal>ftp::ProxyLogin</literal> setzen. Dieser Eintrag gibt die Befehle
an, die gesendet werden müssen, um dem Proxy-Server mitzuteilen, womit er
sich verbinden soll. Ein Beispiel, wie das funktioniert, finden Sie unter
&configureindex;. Die Platzhaltervariablen, die für den zugehörigen
URI-Bestandteil stehen, sind <literal>$(PROXY_USER)</literal>,
<literal>$(PROXY_PASS)</literal>, <literal>$(SITE_USER)</literal>,
<literal>$(SITE_PASS)</literal>, <literal>$(SITE)</literal> und
<literal>$(SITE_PORT)</literal>.</para>

     <para>Die Option <literal>timeout</literal> stellt den Zeitnehmer, der von der
Methode benutzt wird. Dieser Wert wird auf die Verbindungs- sowie die
Datenzeitüberschreitungen angewendet.</para>

     <para>Mehrere Einstellungen werden zum Steuern des passiven Modus
bereitgestellt. Generell ist es sicher, den passiven Modus eingeschaltet zu
lassen, er funktioniert in nahezu jeder Umgebung. Jedoch erfordern einige
Situationen, dass der passive Modus ausgeschaltet und stattdessen
Port-Modus-FTP benutzt wird. Dies kann global eingestellt werden oder für
Verbindungen, die durch einen Proxy oder zu einem bestimmten Rechner gehen
(Beispiele finden Sie in der Musterkonfigurationsdatei).</para>

     <para>Es ist möglich, FTP über HTTP zu leiten, indem die Umgebungsvariable
<envar>ftp_proxy</envar> auf eine HTTP-URL gesetzt wird – lesen Sie die
Besprechung der HTTP-Methode oberhalb bezüglich der Syntax. Sie können dies
nicht in der Konfigurationsdatei setzen und es wird wegen der geringen
Effizienz nicht empfohlen, FTP über HTTP zu benutzen.</para>

     <para>Die Einstellung <literal>ForceExtended</literal> steuert die Benutzung der
RFC-2428-Befehle <literal>EPSV</literal> und <literal>EPRT</literal>. Die
Vorgabe ist »false«, was bedeutet, dass diese Befehle nur benutzt werden,
wenn die Steuerverbindung IPv6 ist. Durch Stellen auf »true« wird die
Benutzung selbst auf IPv4-Verbindungen erzwungen. Beachten Sie, dass die
wenigsten FTP-Server RFC 2428 unterstützen.</para></listitem>
     </varlistentry>

     <varlistentry><term><option>cdrom</option></term>
     <listitem><para>
     Für URIs, die die Methode <literal>cdrom</literal> verwenden, ist die
einzige Option der Einhängepunkt, <literal>cdrom::Mount</literal>, der der
Einhängepunkt des CD-ROM-Laufwerks sein muss (oder der DVD oder was auch
immer), wie er in <filename>/etc/fstab</filename> angegeben wurde. Es ist
möglich, alternative Ein- und Aushängebefehle anzugeben, falls Ihr
Einhängepunkt nicht in der fstab aufgeführt werden kann. Die Syntax besteht
darin, <literallayout>/cdrom/::Mount "foo";</literallayout> in den
<literal>cdrom</literal>-Block einzufügen. Der abschließende Schrägstrich
ist wichtig. Aushängebefehle können per UMount angegeben werden.
     </para></listitem>
     </varlistentry>

     <varlistentry><term><option>gpgv</option></term>
     <listitem><para>
     Die einzige Konfigurationsoption für GPGV-URIs ist
<literal>gpgv::Options</literal>, um zusätzliche Parameter an Gpgv
weiterzuleiten.
     </para></listitem>
     </varlistentry>

     <varlistentry><term><option>CompressionTypes</option></term>
     <listitem><para>Liste der Kompressionstypen, die von den »Beschaffungsmethoden verstanden
werden. Dateien wie <filename>Packages</filename> können in verschiedenen
Kompressionsformaten verfügbar sein. Standardmäßig können die
»Beschaffungsmethoden viele gebräuchliche Formate wie <command>xz</command>-
und <command>gzip</command>-komprimierte Dateien dekomprimieren und erneut
komprimieren. In diesem Bereich können die unterstützten Formate abgefragt,
verändert sowie Unterstützung für weitere Formate hinzugefügt werden (siehe
auch <option>APT::Compressor</option>). Die Syntax dafür lautet:
<synopsis>Acquire::CompressionTypes::<replaceable>Dateierweiterung</replaceable> "<replaceable>Methodenname</replaceable>";</synopsis>
     </para><para>Außerdem kann die Untergruppe <literal>Order</literal> benutzt werden, um zu
definieren, in welcher Reihenfolge das »Beschaffungssystem« die
komprimierten Dateien herunterzuladen versucht. Das »Acquire«-System wird
die erste ausprobieren und bei einem Fehler mit dem nächsten Kompressionstyp
in dieser Liste fortfahren. Um daher einen Typ einem anderen vorzuziehen,
fügen Sie einfach den bevorzugten Typ an erster Stelle in die Liste ein -
Typen, die noch nicht hinzugefügt wurden, werden vorbehaltlos an das Ende
der Liste angehängt, so kann z.B. <synopsis>Acquire::CompressionTypes::Order:: "gz";</synopsis>
benutzt werden, um <command>gzip</command>-komprimierten Dateien den Vorzug
gegenüber allen anderen Formaten zu geben. Falls <command>xz</command>
gegenüber <command>gzip</command> und <command>bzip2</command> den Vorzug
erhalten soll, sollte die Konfigurationseinstellung so aussehen:
<synopsis>Acquire::CompressionTypes::Order { "xz"; "gz"; };</synopsis>. Es ist nicht nötig,
<literal>bz2</literal> explizit zur Liste hinzuzufügen, da es automatisch
hinzufügt wird.</para>
     <para>Beachten Sie, dass
<literal>Dir::Bin::<replaceable>Methodenname</replaceable></literal> zur
Laufzeit geprüft wird. Falls diese Option gesetzt ist und die Unterstützung
für dieses Format nicht direkt in APT eingebaut wurde, wird die Methode nur
benutzt, wenn die Datei existiert, z.B. ist die (integrierte) Einstellung
für die <literal>bzip2</literal>-Methode: <literallayout>Dir::Bin::bzip2 "/bin/bzip2";</literallayout>. Beachten Sie auch, dass auf der Befehlszeile eingegebene Einträge
an das Ende der Liste angehängt werden, die in den Konfigurationsdateien
angegeben wurde, aber vor den Standardeinträgen. Um einen Typ in diesem Fall
gegenüber einem, der über die Konfigurationsdatei angegebenen wurde, zu
bevorzugen, können Sie diese Option direkt setzen – nicht im
Listenstil. Dies wird die definierte Liste nicht außer Kraft setzen, es wird
diesen Typ nur vor die Liste setzen.</para>
     <para>Der besondere Typ <literal>uncompressed</literal> kann benutzt werden, um
unkomprimierten Dateien einen Vorrang zu geben, beachten Sie jedoch, dass
die meisten Archive keine unkomprimierten Dateien bereitstellen, so dass
dies meist nur für lokale Spiegel benutzt werden kann.</para></listitem>
     </varlistentry>

     <varlistentry><term><option>GzipIndexes</option></term>
	 <listitem><para>
	 Wenn <literal>gzip</literal>-komprimierte Indexe heruntergeladen werden
(Packages, Sources oder Translations), speichere sie lokal mit
gzip-Komprimierung. Dies spart eine Menge Festplattenplatz, aber benötigt
mehr CPU-Ressourcen bei der Erstellung des lokalen
Paketzwischenspeicher. Vorgabe ist »false«.
	 </para></listitem>
     </varlistentry>

     <varlistentry><term><option>Languages</option></term>
     <listitem><para>Der Unterabschnitt Languages steuert, welche
<filename>Translation</filename>-Dateien heruntergeladen werden und in
welcher Reihenfolge APT versucht, die Beschreibungsübersetzungen
anzuzeigen. APT wird versuchen, die erste verfügbare Beschreibung für die
zuerst aufgelistete Sprache anzuzeigen. Sprachen können durch ihre kurzen
oder langen Sprachcodes definiert sein. Beachten Sie, dass nicht alle
Archive <filename>Translation</filename>-Dateien für jede Sprache
bereitstellen – insbesondere sind die langen Sprachcodes selten.</para>
     <para>Die Standardliste beinhaltet »environment« und
»en«. »<literal>environment</literal>« hat hier eine besondere Bedeutung: Es
wird zur Laufzeit durch die Sprachcodes ersetzt, die aus der
Umgebungsvariable <literal>LC_MESSAGES</literal> extrahiert wurden. Es wird
außerdem sicherstellen, dass diese Codes nicht zweimal in der Liste
enthalten sind. Falls <literal>LC_MESSAGES</literal> auf »C« gesetzt ist,
wird nur die Datei <filename>Translation-en</filename> (falls verfügbar)
benutzt. Um APT zu zwingen, keine Übersetzungsdatei zu verwenden, benutzen
Sie die Einstellung
<literal>Acquire::Languages=none</literal>. »<literal>none</literal>« ist
ein weiterer Code mit besonderer Bedeutung, der die Suche nach einer
passenden <filename>Translation</filename>-Datei stoppen wird. Dies weist
APT an, diese Übersetzungen auch herunterzuladen, ohne sie tatsächlich zu
verwenden, es sei denn, die Umgebungsvariable gibt diese Sprachen an. Daher
wird die folgende Beispielkonfiguration in einer englischen
Spracheinstellung zu der Reihenfolge »en,de« und in einer deutschen
Spracheinstellung zu »de,en« führen. Beachten Sie, dass »fr«
heruntergeladen, aber nur benutzt wird, falls APT in einer französischen
Spracheinstellung benutzt wird (wobei die Reihenfolge »fr, de, en«
wäre). <programlisting>Acquire::Languages { "environment"; "de"; "en"; "none"; "fr"; };</programlisting></para>
     <para>Hinweis: Um Problemen vorzubeugen, die daher kommen, dass APT in
unterschiedlichen Umgebungen ausgeführt wird (z.B. durch verschiedene
Benutzer oder durch andere Programme) werden alle Translation-Dateien, die
in <filename>/var/lib/apt/lists/</filename> gefunden werden, an das Ende der
Liste hinzugefügt (nach einem impliziten »<literal>none</literal>«).</para>
     </listitem>
     </varlistentry>

     <varlistentry><term><option>ForceIPv4</option></term>
	 <listitem><para>
         Beim Herunterladen wird die Verwendung des IPv4-Protokolls erzwungen.
	 </para></listitem>
     </varlistentry>

     <varlistentry><term><option>ForceIPv6</option></term>
	 <listitem><para>
         Beim Herunterladen wird die Verwendung des IPv6-Protokolls erzwungen.
	 </para></listitem>
     </varlistentry>

     <varlistentry><term><option>MaxReleaseFileSize</option></term>
	 <listitem><para>
           die maximale Dateigröße von Release-/Release.gpg-/InRelease-Dateien. Die
Voreinstellung ist 10 MB.
	 </para></listitem>
     </varlistentry>

     <varlistentry><term><option>EnableSrvRecords</option></term>
	 <listitem><para>
           Diese Option steuert, ob APT den in RFC 2782 spezifizierten
DNS-SRV-Serverdatensatz benutzt, um einen alternativen Server zur Verbindung
auszuwählen. Die Voreinstellung ist »true«.
	 </para></listitem>
     </varlistentry>

     <varlistentry><term><option>AllowInsecureRepositories</option></term>
	 <listitem><para>
	   Ermöglicht Aktualisierungsaktionen, Datendateien aus Depots ohne
ausreichende Sicherheitsinformationen zu laden. Die Voreinstellung ist
»<literal>false</literal>«. Konzept, Auswirkungen sowie Alternativen werden
ausführlich in &apt-secure; erklärt.
	 </para></listitem>
     </varlistentry>

     <varlistentry><term><option>AllowWeakRepositories</option></term>
	 <listitem><para>
	   Ermöglicht Aktualisierungsaktionen, um Datendateien aus Depots zu laden, die
Sicherheitsinformationen bereitstellen, die jedoch nicht mehr als
kryptografisch stark genug erachtet werden. Die Voreinstellung ist
»<literal>false</literal>«. Konzept, Auswirkungen sowie Alternativen werden
ausführlich in &apt-secure; erklärt.
	 </para></listitem>
     </varlistentry>

     <varlistentry><term><option>AllowDowngradeToInsecureRepositories</option></term>
	 <listitem><para>
	   Ermöglicht, dass ein Depot, das vorher per GPG signiert war, während einer
Aktualisierungstransaktion seine Signatur verliert. Wenn ein vorher
vertrauenswürdiges Depot keine gültige Signatur mehr hat, wird APT die
Aktualisierung verweigern. Diese Option kann verwendet werden, um diesen
Schutz außer Kraft zu setzen. Sie wollen dies wahrscheinlich nahezu nie
aktivieren. Die Voreinstellung ist »<literal>false</literal>«. Konzept,
Auswirkungen sowie Alternativen werden ausführlich in &apt-secure; erklärt.
	 </para></listitem>
     </varlistentry>

     <varlistentry><term><option>Changelogs::URI</option> Gültigkeitsbereich</term>
	<listitem><para>
	  Changelogs können nur beschafft werden, falls ein URI bekannt ist, von dem
sie geholt werden können. Vorzugsweise gibt die Release-Datei ihn in einem
»Changelogs«-Feld an. Falls dies nicht verfügbar ist, werden die Felder
»Label« und »Origin« der Release-Datei benutzt, um zu prüfen, ob eine
<literal>Acquire::Changelogs::URI::Label::<replaceable>BESCHRIFTUNG</replaceable></literal>-
oder
<literal>Acquire::Changelogs::URI::Origin::<replaceable>URSPRUNG</replaceable></literal>-Option
existiert und, falls dies der Fall ist, wird dieser Wert genommen. Der Wert
in der Release-Datei kann mit
<literal>Acquire::Changelogs::URI::Override::Label::<replaceable>BESCHRIFTUNG</replaceable></literal>
oder
<literal>Acquire::Changelogs::URI::Override::Origin::<replaceable>URSPRUNG</replaceable></literal>
außer Kraft gesetzt werden. Der Wert sollte ein normaler URI auf eine
Textdatei sein, außer dass paketspezifische Daten durch den Platzhalter
<literal>@CHANGEPATH@</literal> ersetzt werden. Der Wert dafür ist: 1. Falls
das Paket von einem Bestandteil stammt (z.B. <literal>main</literal>), ist
dies der erste Teil, ansonsten wird er weggelassen. 2. der erste Buchstabe
des Quellpaketnamens, es sei denn, der Paketname beginnt mit
»<literal>lib</literal>«. In diesem Fall werden es die vier ersten
Buchstaben sein. 3. der komplette Quellpaketname. 4. wieder der vollständige
Name und 5. die Quellversion. Der erste (falls vorhanden), zweite, dritte
und vierte Teil werden durch einen Schrägstrich (»<literal>/</literal>«)
voneinander getrennt und zwischen dem vierten und fünften ist ein
Unterstrich (»<literal>_</literal>«). Der Spezialwert
»<literal>no</literal>«, der für diese Option verfügbar ist, gibt an, dass
diese Quelle nicht benutzt werden kann, um von dort Changelog-Dateien zu
beschaffen. In diesem Fall wird, falls verfügbar, eine andere Quelle
ausprobiert.
	</para></listitem>
    </varlistentry>

   </variablelist>
 </refsect1>

 <refsect1><title>Programmspezifische Konfiguration</title>
    <para>Insbesondere kann es mit der Einführung des <command>apt</command>-Programms
nützlich sein, bestimmte Optionen nur für ein bestimmtes Programm zu setzen,
da sogar Optionen, die aussehen, als ob sie nur ein bestimmtes Programm
beträfen, wie <option>APT::Get::Show-Versions</option>, sowohl
<command>apt-get</command> als auch <command>apt</command> beeinflussen.
    </para>
    <para>Eine Option alleine für ein bestimmtes Programm zu setzen kann erreicht
werden, indem die Option innerhalb des Gültigkeitsbereichs
<option>Binary::<replaceable>bestimmtes_Programm</replaceable></option>
gesetzt wird. Das Setzen der Option <option>APT::Get::Show-Versions</option>
für ausschließlich <command>apt</command> kann z.B. stattdessen durch Setzen
von <option>Binary::apt::APT::Get::Show-Versions</option> erledigt werden.</para>
    <para>Beachten Sie, dass Sie, wie im Abschnitt BESCHREIBUNG weiter oben gesehen,
die programmspezifischen Optionen weder auf der Befehlszeile selbst, noch in
Konfigurationsdateien, die über die Befehlszeile geladen werden, setzen
können.</para>
 </refsect1>

 <refsect1><title>Verzeichnisse</title>

   <para>Der <literal>Dir::State</literal>-Abschnitt hat Verzeichnisse, die zu
lokalen Statusinformationen gehören. <literal>lists</literal> ist das
Verzeichnis, in das heruntergeladene Paketlisten abgelegt werden und
<literal>status</literal> ist der Name der
&dpkg;-Statusdatei. <literal>preferences</literal> ist der Name der
APT-<filename>preferences</filename>-Datei. <literal>Dir::State</literal>
enthält das Standardverzeichnis, das allen Unterelementen vorangestellt
wird, falls sie nicht mit <filename>/</filename> oder
<filename>./</filename> beginnen.</para>

   <para><literal>Dir::Cache</literal> enthält Orte, die zu lokalen
Zwischenspeicherinformationen gehören, so wie die beiden
Paketzwischenspeicher <literal>srcpkgcache</literal> und
<literal>pkgcache</literal>, sowie den Ort, an den heruntergeladene Archive
abgelegt werden, <literal>Dir::Cache::archives</literal>. Die Generierung
von Zwischenspeichern kann ausgeschaltet werden, indem
<literal>pkgcache</literal> oder <literal>srcpkgcache</literal> auf
<literal>""</literal> gesetzt wird. Dies wird den Start verlangsamen, aber
Plattenplatz sparen. Es ist vermutlich vorzuziehen, statt des »srcpkgcache«s
den »pkgcache« auszuschalten. Wie <literal>Dir::State</literal> ist das
Standardverzeichnis in <literal>Dir::Cache</literal> enthalten.</para>

   <para><literal>Dir::Etc</literal> enthält den Ort der Konfigurationsdateien,
<literal>sourcelist</literal> gibt den Ort der Quellliste und
<literal>main</literal> ist die Standardkonfigurationsdatei (Einstellung hat
keine Auswirkung, außer wenn sie aus der in <envar>APT_CONFIG</envar>
angegebenen Konfigurationsdatei erfolgt).</para>

   <para>Die <literal>Dir::Parts</literal>-Einstellung liest alle Konfigurationsteile
in lexikalischer Reihenfolge vom angegebenen Verzeichnis ein. Nachdem dies
geschehen ist, wird die Hauptkonfigurationsdatei geladen.</para>

   <para>Auf Programme wird von <literal>Dir::Bin</literal>
verwiesen. <literal>Dir::Bin::Methods</literal> gibt den Ort des
Methodensteuerungsprogramms an und <literal>gzip</literal>,
<literal>bzip2</literal>, <literal>lzma</literal>, <literal>dpkg</literal>,
<literal>apt-get</literal>, <literal>dpkg-source</literal>,
<literal>dpkg-buildpackage</literal> und <literal>apt-cache</literal> geben
den Ort des jeweiligen Programms an.</para>

   <para>
     Das Konfigurationselement <literal>RootDir</literal> hat eine besondere
Bedeutung. Falls es gesetzt ist, sind alle Pfad relativ zu
<literal>RootDir</literal>, <emphasis>sogar Pfade, die absolut angegeben
wurden</emphasis>. So wird zum Beispiel, wenn <literal>RootDir</literal> auf
<filename>/tmp/staging</filename> und <literal>Dir::State::status</literal>
auf <filename>/var/lib/dpkg/status</filename> gesetzt ist, nach der
Statusdatei in <filename>/tmp/staging/var/lib/dpkg/status</filename>
nachgesehen. Falls Sie etwas nur relativen Pfaden voranstellen möchten,
setzen Sie stattdessen <literal>Dir</literal>.
   </para>

   <para>
      Die Liste <literal>Ignore-Files-Silently</literal> kann benutzt werden, um
anzugeben, welche Dateien APT beim Auswerten der Dateien in den
Fragment-Verzeichnissen stillschweigend ignorieren sollte. Standardmäßig
werden Dateien, die auf <literal>.disabled</literal>, <literal>~</literal>,
<literal>.bak</literal> oder <literal>.dpkg-[a-z]+</literal> enden
stillschweigend ignoriert. Wie beim letzten Vorgabewert gesehen, kann die
Syntax für reguläre Ausdrücke für diese Muster verwandt werden.
   </para>
 </refsect1>
 
 <refsect1><title>APT in DSelect</title>
   <para>   
   Wenn APT als eine &dselect;-Methode benutzt wird, steuern mehrere
Konfigurationsdirektiven das Standardverhalten. Diese stehen im Abschnitt
<literal>DSelect</literal>.</para>
   
   <variablelist>
     <varlistentry><term><option>Clean</option></term>
     <listitem><para>Zwischenspeicherbereinigungsmodus; dieser Wert kann entweder
»<literal>always</literal>«, »<literal>prompt</literal>«,
»<literal>auto</literal>«, »<literal>pre-auto</literal>« oder
»<literal>never</literal>« sein. »<literal>always</literal>« und
»<literal>prompt</literal>« werden, nachdem das Upgrade durchgeführt wurde,
alle Pakete aus dem Zwischenspeicher entfernen, »<literal>prompt</literal>«
(die Vorgabe) tut dies bedingt. »<literal>auto</literal>« entfernt nur jene
Pakete, die nicht länger heruntergeladen werden können (zum Beispiel, weil
sie durch eine neue Version ersetzt wurden). »<literal>pre-auto</literal>«
führt diese Aktion vor dem Herunterladen neuer Pakete durch.</para></listitem>
     </varlistentry>

     <varlistentry><term><option>options</option></term>
     <listitem><para>Die Inhalte dieser Variablen werden als Befehlszeilenoptionen an &apt-get;
übermittelt, wenn es für die Installationsphase durchlaufen wird.</para></listitem>
     </varlistentry>

     <varlistentry><term><option>Updateoptions</option></term>
     <listitem><para>Die Inhalte dieser Variable werden als Befehlszeilenoptionen an &apt-get;
übermittelt, wenn es für die Aktualisierungsphase durchlaufen wird.</para></listitem>
     </varlistentry>

     <varlistentry><term><option>PromptAfterUpdate</option></term>
     <listitem><para>Falls »true«, wird die Aktualisierungsaktion [U] in &dselect; immer
nachfragen, um fortzufahren. Vorgabe ist es, nur bei Fehlern nachzufragen.</para></listitem>
     </varlistentry>
   </variablelist>
 </refsect1>
 
 <refsect1><title>Wie APT &dpkg; aufruft</title>
   <para>Mehrere Konfigurationsdirektiven steuern, wie APT &dpkg; aufruft. Diese
stehen im Abschnitt <literal>DPkg</literal>.</para>

   <variablelist>
     <varlistentry><term><option>options</option></term>
     <listitem><para>Dies ist eine Liste von Optionen, die an &dpkg; übermittelt werden. Die
Optionen müssen unter Benutzung der Listenschreibweise angegeben werden und
jedes Listenelement wird als einzelnes Argument an &dpkg; übermittelt.</para></listitem>
     </varlistentry>

     <varlistentry><term><option>Path</option></term>
     <listitem><para>This is a string that defines the <envar>PATH</envar> environment variable
used when running dpkg. It may be set to any valid value of that environment
variable; or the empty string, in which case the variable is not changed.</para></listitem>
     </varlistentry>
     
     <varlistentry><term><option>Pre-Invoke</option></term><term><option>Post-Invoke</option></term>
     <listitem><para>Dies ist eine Liste von Shell-Befehlen, die vor/nach dem Aufruf von &dpkg;
ausgeführt werden. Wie <literal>options</literal> muss dies in
Listenschreibweise angegeben werden. Die Befehle werden der Reihenfolge nach
mit <filename>/bin/sh</filename> aufgerufen, sollte einer fehlschlagen, wird
APT abgebrochen.</para></listitem>
     </varlistentry>

     <varlistentry><term><option>Pre-Install-Pkgs</option></term>
     <listitem><para>Dies ist eine Liste von Shell-Befehlen, die vor dem Aufruf von &dpkg;
ausgeführt werden. Wie <literal>options</literal> muss dies in
Listenschreibweise angegeben werden. Die Befehle werden der Reihenfolge nach
mit <filename>/bin/sh</filename> aufgerufen, sollte einer fehlschlagen, wird
APT abgebrochen. APT wird den Befehlen die Dateinamen aller .deb-Dateien,
die es installieren wird, einen pro Zeile, an den angeforderten
standardmäßig auf die Standardeingabe verweisenden Dateideskriptor,
übergeben.</para>

     <para>Version 2 dieses Protokolls sendet mehr Informationen durch den
angeforderten Dateideskriptor: eine Zeile mit dem Text <literal>VERSION
2</literal>, den APT-Konfigurationsraum und eine Liste von Paketaktionen mit
Dateinamen und Versionsinformationen.</para>

     <para>Jede Zeile der Konfigurationsrichtlinien hat die Form
<literal>Schlüssel=Wert</literal>. Sonderzeichen (Gleichheitszeichen,
Zeilenumbrüche, nicht druckbare Zeichen, Anführungszeichen und
Prozentzeichen in <literal>Schlüssel</literal> und Zeilenumbrüche, nicht
druckbare Zeichen und Prozentzeichen in <literal>Wert</literal>) werden
%-kodiert. Listen werden durch mehrere
<literal>Schlüssel=Wert</literal>-Zeilen mit demselben Schlüssel
dargestellt. Der Konfigurationsabschnitt endet mit einer leeren Zeile.</para>

     <para>Paketaktionszeilen bestehen in Version 2 aus fünf Feldern: Paketname (ohne
Architektureignung, sogar wenn fremd), alte Version, Richtung der
Versionsänderung (»&lt;« für Upgrades, »&gt;« für Downgrades, »=« für keine
Änderung), neue Version, Aktion. Die Versionsfelder sind »-« für gar keine
Version (zum Beispiel, wenn ein Paket zum ersten Mal installiert wird; keine
Version wird als älter als jede echte Version angesehen, daher wird ein
Upgrade als <literal>- &lt; 1.23.4</literal> angezeigt). Das Aktionsfeld ist
»**CONFIGURE**«, falls das Paket konfiguriert wird, »**REMOVE**«, falls es
entfernt wird oder der Dateiname einer .deb-Datei, falls es entpackt wird.</para>

     <para>In Version 3 folgt nach jedem Versionsfeld die Architektur dieser
Version. Sie ist »-«, falls es dort keine Version gibt, und ein Feld, das
die Multiarch-Typen »same«, »foreign«, »allowed« oder »none«
anzeigt. Beachten Sie, dass »none« kein korrekter Typenname ist. Er wird nur
beibehalten, um kompatibel zu bleiben. Er sollte als »no« gelesen werden und
Benutzer werden ermutigt, beides zu unterstützen.</para>

     <para>Die Protokollversion, die für den Befehl
<literal><replaceable>Befehl</replaceable></literal> benutzt werden soll,
kann durch entsprechendes Setzen von
<literal>DPkg::Tools::options::<replaceable>Befehl</replaceable>::Version</literal>
ausgewählt werden, Voreinstellung ist Version 1. Falls APT die angefragte
Version nicht unterstützt, wird es stattdessen die Informationen in der
höchsten Version senden, für die es Unterstützung bietet.
     </para>

     <para>Der Dateideskriptor, der zum Senden der Informationen benutzt wird, kann mit
<literal>DPkg::Tools::options::<replaceable>Befehl</replaceable>::InfoFD</literal>
abgefragt werden. Er ist standardmäßig <literal>0</literal> für die
Standardeingabe und seit Version 0.9.11 verfügbar. Sie können durch Prüfen
der Umgebungsvariable <envar>APT_HOOK_INFO_FD</envar> ermitteln, ob die
Option untersützt wird. Sie enthält die Nummer des verwendeten
Dateideskriptors als eine Bestätigung.</para>
     </listitem>
     </varlistentry>

     <varlistentry><term><option>Run-Directory</option></term>
     <listitem><para>APT wechselt mit chdir in dieses Verzeichnis, bevor &dpkg; aufgerufen wird,
die Vorgabe ist <filename>/</filename>.</para></listitem>
     </varlistentry>

     <varlistentry><term><option>Build-options</option></term>
     <listitem><para>Diese Optionen werden an &dpkg-buildpackage; beim Kompilieren von Paketen
übermittelt. Standardmäßig wird das Signieren ausgeschaltet und alle
Programme werden erstellt.</para></listitem>
     </varlistentry>

     <varlistentry><term><option>DPkg::ConfigurePending</option></term>
     <listitem><para>Wenn diese Option gesetzt ist, wird APT <command>dpkg --configure
--pending</command> aufrufen, um &dpkg; alle benötigten Konfigurationen und
Trigger handhaben zu lassen. Diese Option ist als Vorgabe aktiviert, sie zu
Deaktivieren könnte jedoch nützlich sein, wenn Sie APT mehrmals
hintereinander ausführen möchten – z.B. in einem Installationsprogramm. In
diesem Szenario könnten Sie diese Option in allen außer der letzten
Ausführung deaktivieren.</para></listitem>
     </varlistentry>
   </variablelist>
 </refsect1>

 <refsect1>
   <title>Periodische- und Archivoptionen</title>
   <para><literal>APT::Periodic</literal>- und
<literal>APT::Archives</literal>-Gruppen von Optionen konfigurieren das
Verhalten periodischer APT-Aktualisierungen, die vom Skript
<literal>/usr/lib/apt/apt.systemd.daily</literal> durchgeführt werden. Lesen
Sie den Anfang dieses Skripts, um eine kurze Dokumentation dieser Optionen
zu erhalten.
   </para>
 </refsect1>

 <refsect1>
   <title>Fehlersuchoptionen</title>
   <para>
     Einschalten von Optionen im Abschnitt <literal>Debug::</literal> wird
veranlassen, dass Fehlersuchinformationen an die Standardfehlerausgabe des
Programms gesendet werden, das die <literal>apt</literal>-Bibliotheken
benutzt oder besondere Programmmodi einschaltet, die in erster Linie für das
Fehlersuchverhalten von <literal>apt</literal> nützlich sind. Die meisten
dieser Optionen sind für den normalen Benutzer uninteressant, aber ein paar
könnten es sein:

     <itemizedlist>
       <listitem>
	 <para>
	   <literal>Debug::pkgProblemResolver</literal> schaltet die Ausgabe über die
von <literal>dist-upgrade, upgrade, install, remove, purge</literal>
getroffenen Entscheidungen ein.
	 </para>
       </listitem>

       <listitem>
	 <para>
	   <literal>Debug::NoLocking</literal> schaltet jegliches Sperren von Dateien
aus. Dies kann benutzt werden, um einige Aktionen (zum Beispiel
<literal>apt-get -s install</literal>) als nicht root-Benutzer auszuführen.
	 </para>
       </listitem>

       <listitem>
	 <para>
	   <literal>Debug::pkgDPkgPM</literal> gibt die tatsächliche Befehlszeile
jedesmal aus, wenn <literal>apt</literal> &dpkg; aufruft.
	 </para>
       </listitem>

       <listitem>
	 <para>
	   <!-- TODO: provide a
	   motivating example, except I haven't a clue why you'd want
	   to do this. -->
<literal>Debug::IdentCdrom</literal> schaltet das Einbeziehen von
statfs-Daten in CD-ROM-IDs aus.  
	 </para>
       </listitem>
     </itemizedlist>
   </para>

   <para>
     Eine vollständige Liste der Fehlersuchoptionen von APT folgt.
   </para>

   <variablelist>
     <varlistentry>
       <term><option>Debug::Acquire::cdrom</option></term>

       <listitem>
	 <para>
	   gibt Informationen aus, die sich auf Zugriffe von
<literal>cdrom://</literal>-Quellen beziehen.
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::Acquire::ftp</option></term>

       <listitem>
	 <para>
	   gibt Informationen aus, die sich auf das Herunterladen von Paketen per FTP
beziehen.
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::Acquire::http</option></term>

       <listitem>
	 <para>
	   gibt Informationen aus, die sich auf das Herunterladen von Paketen per HTTP
beziehen.
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::Acquire::https</option></term>

       <listitem>
	 <para>
	   gibt Informationen aus, die sich auf das Herunterladen von Paketen per HTTPS
beziehen.
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::Acquire::gpgv</option></term>

       <listitem>
	 <para>
	   gibt Informationen aus, die sich auf das Prüfen kryptografischer Signaturen
mittels <literal>gpg</literal> beziehen.
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::aptcdrom</option></term>

       <listitem>
	 <para>
	   gibt Informationen über den Zugriffsprozess auf Paketsammlungen aus, die auf
CD-ROMs gespeichert sind.
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::BuildDeps</option></term>
       <listitem>
	 <para>
	   beschreibt den Prozess der Auflösung von Bauabhängigkeiten in &apt-get;.
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::Hashes</option></term>
       <listitem>
	 <para>
	   gibt jeden kryptografischen Hash aus, der von den
<literal>apt</literal>-Bibliotheken generiert wurde.
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::IdentCDROM</option></term>
       <listitem>
	 <para>
	   schließt keine Informationen von <literal>statfs</literal> ein, und zwar die
Anzahl der benutzten und freien Blöcke auf dem CD-ROM-Dateisystem, wenn eine
ID für eine CD-ROM generiert wird.
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::NoLocking</option></term>
       <listitem>
	 <para>
	   schaltet jegliches Sperren von Dateien aus. Dies wird zum Beispiel erlauben,
dass zwei Instanzen von <quote><literal>apt-get update</literal></quote> zur
gleichen Zeit laufen.
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::pkgAcquire</option></term>

       <listitem>
	 <para>
	   protokolliert, wenn Elemente in die globalen Warteschlange zum Herunterladen
hinzugefügt oder aus ihr entfernt werden.
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::pkgAcquire::Auth</option></term>
       <listitem>
	 <para>
	   gibt Statusmeldungen und Fehler aus, die sich auf das Prüfen von Prüfsummen
und kryptografischen Signaturen von heruntergeladenen Dateien beziehen.
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::pkgAcquire::Diffs</option></term>
       <listitem>
	 <para>
	   gibt Informationen über das Herunterladen und Anwenden von
Paketindexlisten-Diffs und Fehler, die die Paketindexlisten-Diffs betreffen,
aus.
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::pkgAcquire::RRed</option></term>

       <listitem>
	 <para>
	   gibt Informationen aus, die sich auf das Patchen von Paketlisten von APT
beziehen, wenn Index-Diffs anstelle vollständiger Indexe heruntergeladen
werden.
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::pkgAcquire::Worker</option></term>

       <listitem>
	 <para>
	   protokolliert alle Interaktionen mit Unterprozessen, die das eigentliche
Herunterladen durchführen.
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::pkgAutoRemove</option></term>

       <listitem>
	 <para>
	   protokolliert alle Ereignisse, die sich auf den
automatisch-installiert-Status von Paketen und auf das Entfernen von nicht
benutzten Paketen beziehen.
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::pkgDepCache::AutoInstall</option></term>
       <listitem>
	 <para>
	   generiert Fehlersuchmeldungen, die beschreiben, welche Pakete automatisch
installiert werden, um Abhängigkeiten aufzulösen. Dies entspricht dem
anfangs durchgeführten auto-install-Durchlauf, z.B. in <literal>apt-get
install</literal> und nicht dem vollständigen
<literal>apt</literal>-Abhängigkeitsauflöser. Lesen Sie dafür
<literal>Debug::pkgProblemResolver</literal>.
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::pkgDepCache::Marker</option></term>
       <listitem>
        <para>
           generiert Fehlersuchmeldungen, die beschreiben, welches Paket als
»keep«/»install«/»remove« markiert ist, während der ProblemResolver seine
Arbeit verrichtet. Jedes Hinzufügen oder Löschen kann zusätzliche Aktionen
auslösen. Sie werden nach zwei eingerückten Leerzeichen unter dem
Originaleintrag angezeigt. Jede Zeile hat das Format
<literal>MarkKeep</literal>, <literal>MarkDelete</literal> oder
<literal>MarkInstall</literal> gefolgt von <literal>Paketname &lt;a.b.c
-&gt; d.e.f | x.y.z&gt; (Abschnitt)</literal> wobei <literal>a.b.c</literal>
die aktuelle Version des Paketes, <literal>d.e.f</literal> die Version, die
zur Installation vorgesehen und <literal>x.y.z</literal> eine neuere
Version, die aber nicht zur Installation vorgesehen ist (aufgrund einer
niedrigen Pinning-Bewertung). Die letzten beiden können weggelassen werden,
wenn es keine gibt oder wenn sie die gleiche Version haben, wie die, die zur
installierten Version identisch ist. <literal>Abschnitt</literal> ist der
Name des Abschnitts, in dem das Paket erscheint.
        </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::pkgDPkgPM</option></term>
       <listitem>
	 <para>
	   gibt, wenn &dpkg; aufgerufen wird, die genauen Befehlszeile mit der es
aufgerufen wurde, mit Argumenten, die durch einzelne Leerzeichen getrennt
sind, aus.
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::pkgDPkgProgressReporting</option></term>
       <listitem>
	 <para>
	   gibt alle von &dpkg; über einen Status-Datei-Deskriptor empfangenen Daten
und alle während deren Auswertung gefundenen Fehler aus.
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::pkgOrderList</option></term>

       <listitem>
	 <para>
	   generiert eine Aufzeichnung des Algorithmus, der über die Reihenfolge
entscheidet, in der <literal>apt</literal> Pakete an &dpkg; weiterleiten
soll.
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::pkgPackageManager</option></term>

       <listitem>
	 <para>
	   gibt Statusmeldungen aus, die die Schritte nachverfolgen, die beim Aufruf
von &dpkg; ausgeführt werden.
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::pkgPolicy</option></term>

       <listitem>
	 <para>
	   gibt die Priorität jeder Paketliste beim Start aus.
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::pkgProblemResolver</option></term>

       <listitem>
	 <para>
	   verfolgt die Ausführung des Abhängigkeitsauflösers (dies wird nur auf das
angewendet, was geschieht, wenn ein komplexes Abhängigkeitsproblem
aufgetreten ist).
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::pkgProblemResolver::ShowScores</option></term>
       <listitem>
        <para>
          zeigt eine Liste aller installierten Pakete mit ihren berechneten
Bewertungen, die vom pkgProblemResolver benutzt werden, an. Die Beschreibung
des Pakets stimmt mit der in <literal>Debug::pkgDepCache::Marker</literal>
überein.
        </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::sourceList</option></term>

       <listitem>
	 <para>
	   gibt die Informationen über die aus
<filename>/etc/apt/vendors.list</filename> gelesenen Anbieter aus.
	 </para>
       </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>Debug::RunScripts</option></term>
       <listitem>
        <para>
          zeigt die externen Befehle, die durch APT-Hooks aufgerufen werden. Dies
schließt z.B. die Konfigurationsoptionen
<literal>DPkg::{Pre,Post}-Invoke</literal> oder
<literal>APT::Update::{Pre,Post}-Invoke</literal> mit ein.
        </para>
       </listitem>
     </varlistentry>



   <!-- 2009/07/11 Currently used nowhere. The corresponding code
is commented.
     <varlistentry>

       <term><literal>Debug::Vendor</literal></term>

       <listitem>
	 <para>
	   Print information about each vendor.
	 </para>
       </listitem>
     </varlistentry>
-->
</variablelist>
 </refsect1>
 
 <refsect1><title>Beispiele</title>
   <para>&configureindex; ist eine Konfigurationsdatei, die Beispielwerte für alle
möglichen Optionen zeigen.</para>
 </refsect1>
 
 <refsect1><title>Dateien</title>
   <variablelist>
      &file-aptconf;
   </variablelist>
 </refsect1>
 
 <refsect1><title>Siehe auch</title>
   <para><!-- ? reading apt.conf -->
&apt-cache;, &apt-config;, &apt-preferences;.</para>
 </refsect1>

 &manbugs;
 
 <refsect1><title>&translation-title;</title>
   <para>&translation-holder;</para>
   <para>&translation-english;</para>
 </refsect1>
</refentry>

